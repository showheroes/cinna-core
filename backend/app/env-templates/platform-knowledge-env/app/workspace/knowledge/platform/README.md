# Cinna Core

A conversational AI agent platform where users create custom AI agents, run them in isolated Docker environments, and interact through persistent chat sessions. Agent developers publish versioned bundles to a catalog; other users install them. Agents can be scheduled, triggered by email/webhooks, and exposed via A2A and MCP protocols.

**Stack:** FastAPI + PostgreSQL | React + TypeScript + TanStack | Docker isolation | SQLModel ORM

## Core Idea

The platform separates three distinct layers:

- **Agent** (logical definition) — what the agent does: custom prompts, credentials, SDK configuration
- **Environment** (runtime instance) — where it runs: a Docker container with the agent's workspace, tools, and files
- **Session** (conversation) — how users interact: a persistent chat thread with independent message history

One agent can have multiple environments (for testing, production, or rollback via blue-green deployment). One environment can host multiple sessions that share the same file workspace but maintain separate conversation histories.

Agents operate in two modes:
- **Building mode** — development state; agent uses a larger context window, can create/modify scripts and configure integrations
- **Conversation mode** — execution state; agent runs pre-built workflows with a lightweight prompt for faster, cheaper responses

Sessions can be started manually, by automated triggers (CRON, email, webhook), or by other agents via handover. External systems can connect through A2A (Agent-to-Agent protocol) or MCP (Model Context Protocol).

---

## Glossary

| Term | Definition |
|------|-----------|
| **Agent** | User-defined AI assistant with custom prompts, credentials, and SDK configuration |
| **Agent Environment** | Runtime instance (Docker container or remote server) where an agent executes |
| **Session** | Persistent chat conversation between a user and an agent environment |
| **Message** | Single communication unit within a session (user or agent) |
| **Activity** | Logged event or summary of agent actions within a session |
| **Credential** | Encrypted API key or service account used by agents (e.g., Gmail, Odoo) |
| **AI Credential** | LLM provider API key (Claude, OpenAI, etc.) for agent runtime |
| **Knowledge Source** | Git-based repository of documentation agents can query via RAG |
| **Agent Plugin** | Marketplace capability that extends agent functionality |
| **Addon** | The user-facing umbrella over the two things an agent carries beyond its prompt — **plugins** (`AgentPluginLink` rows: marketplace, bundle or skills-catalog) and **agent skills** (`skills/<name>/` folders). Deliberately **presentation-layer only**: it names the agent page's Addons tab, the Add-addon dialog, the admin's Addon Marketplaces page and the `GET /agents/{id}/addons` projection, while every table, service and doc keeps saying `plugin` / `skill` / `SkillPackage`. **A row never says "addon"** — it says plugin or skill. The projection exists because a catalog install is *both* a link and an index entry, so the agent page used to show it twice; the fold is done server-side, once, so the tab, the CLI and any later consumer cannot each invent their own |
| **Input Task** | User-submitted task that goes through refinement before agent execution; extended with short-code IDs, comments, attachments, status history, and subtask delegation |
| **Task Trigger** | Automated rule (CRON, webhook, date) that creates tasks for an agent |
| **Agent Bundle** | Publisher-owned, versioned packaging of an agent identified by a reverse-DNS bundle ID. Publishing an agent creates a bundle; each publish creates an immutable revision |
| **Bundle Revision** | Immutable snapshot of a bundle's workspace content (scripts, docs, knowledge, files, prompts, SDK settings, agent schedules, agent-row metadata: description, example prompts, status refresh command, agent API / A2A / webapp enablement flags, SDK config, and the full A2A capability contract) taken at publish time |
| **Install** | A user's running copy of a published bundle — an `Agent` row seeded from a bundle revision with its own persistent App Data volume |
| **App Data** | Per-user, per-bundle persistent storage volume (`/app/workspace/app-data`) that survives uninstall and reattaches on reinstall |
| **Guest Share** | Token-based, time-limited, revocable link that gives unauthenticated (or authenticated) viewers chat-only access to a specific agent install; protected by a 4-digit security code, scoped to conversation mode, and optionally exposes a read-only environment panel |
| **Handover** | Agent-to-agent task delegation within the platform |
| **Improvement Request** | An `AgentImprovementRequest` row: a consent-gated, one-directional, one-time share of a frozen session snapshot (transcript + tuning-relevant runtime context, including the prompts and MEMORY files that formed the system prompt) from the session owner to the agent's owner — the bundle publisher for a consumer install, or the requester themselves otherwise. Raised via a session's "Improve Agent" menu item or `/session-improve`; reviewed from a Configuration-tab card or `cinna improve` CLI verbs; downloadable as a self-describing ZIP archive. Frozen at consent — nothing reads the source session again — and has no withdrawal in v1. |
| **Workspace** | Isolation boundary for user's agents, sessions, and resources |
| **AI Function** | LLM utility for text generation, classification, extraction with multi-provider cascade fallback |
| **Disclaimer** | An admin-configurable, server-wide Markdown notice shown at login in a non-dismissible modal. Superusers enable/disable it and choose when it appears (`new_users` — once per browser, `every_login` — once per session). Acknowledgement is tracked in versioned browser storage; editing the text or mode increments the version and re-shows the notice to all users. |
| **Access Policy** | The admin-configured front door, stored on the `ServerConfig` singleton: registration mode (`open` / `invite_only`), allowed email patterns, the password and Google sign-in switches, and the default role for new accounts. Resolved by `AccessPolicyService` and never re-derived elsewhere; a public, rate-limited projection drives the login and signup pages. Patterns gate registration only — an existing user is never locked out by an edit. |
| **Superuser Break-Glass** | Superusers keep password login, recovery and reset even when password sign-in is off for everyone else, so a broken Google configuration cannot lock every administrator out of an instance. |
| **Email Confirmation** | A per-account marker (`email_confirmed`) that gates all non-recovery outbound email. New users start unconfirmed and click a link in a confirmation email; Google-OAuth users and superusers are auto-confirmed. Unconfirmed accounts are limited to 5 standalone agents; confirmed accounts are limited to 50; superusers are unlimited. |
| **Invitation** | An administrator's offer of a pre-created, passwordless account (`user_invitation`, one row per account, migration `89878dff89a6`). Created through the `create_account` chokepoint with `AccountOrigin.INVITE` — ungated, so an invite bypasses `allowed_email_patterns` and invite-only mode. Redeemed by setting a password, by signing in with Google, or by completing a password reset; acceptance also confirms the address. The link carries a `purpose="invite"` JWT whose `jti` **is** the row's `token_jti`, so resending rotates it and every earlier link dies. Status is derived, never stored: `accepted` → `revoked` → `expired` → `pending`. |
| **AI Provider** | An `AIProvider` row (table `ai_provider`, surface `/admin/ai-credentials#providers`, API `/admin/ai-providers`): **a source of AI keys plus the rule for who automatically gets one.** Server-scoped and superuser-only; its secret is never returned by any endpoint. Two **kinds** — `fixed_key` (one pasted model key every member holds a copy of) and `minted` (each person gets their own key, created at the vendor through an organisation administration secret; OpenAI only). Holds `auto_provision_roles` (the rule, read at account creation and never on a later role change) plus the whole wiring policy (`set_as_default`, `set_user_sdk_defaults`, `sdk_default_modes`, `default_model`, `available_models`, the per-mode model overrides, `expiry_notification_date`). Owns exactly one Managed AI credential, written with it in one transaction. **"Provider" never means the vendor** — the vendor is the **type** (`AICredentialType`), which is what an adapter describes and what the `Type` column shows. |
| **Managed AI credential** | A `ManagedAICredential` row: **the key that exists and who holds it.** Owns the member list (`ManagedAICredentialMembership`, one row per person) and reconciles it into per-user `AICredential` children. When it belongs to a Provider (`provider_id`) it is read-only for the key and every wiring flag — writes to those are a `400` naming the Provider, and so is deleting it on its own, because that would leave the Provider auto-provisioning with nothing to grant through. With no Provider it is a **manual** record: its own pasted key, its own wiring policy, its own hand-curated members, and no rule — a manual record never auto-provisions. `provisioning_mode` is derived rather than stored (no Provider or `fixed_key` → `shared`; `minted` → `minted`), and the shadowed policy columns are read through one resolver so a record and its Provider cannot disagree. |
| **Admin-managed AI credential** | An `AICredential` row provisioned by a superuser on behalf of a target user (`is_admin_managed=True`) — the per-user child of a Managed AI credential, which in turn may belong to an AI Provider. Owned by the user and fully participates in existing per-user plumbing; read-only through the user-facing CRUD (owner may use and set as default, but cannot edit/delete/re-key). |
| **Account config** | The native-client credential bundle returned by `GET /external/account-config` — a list of provider descriptors with decrypted API keys, consumed by Cinna Desktop/Mobile to auto-create local LLM providers on login. |
| **`current_user` record** | A synthetic, reserved entry appended to every agent environment's `credentials.json` (`id="current_user"`, `type="current_user"`). Built host-side from the install owner's `User` row — carries `username`, `full_name`, `email`, `email_confirmed`, a `custom_details` map of user-authored `UPPER_SNAKE` key/value pairs, and four locale/communication preference fields: `timezone` (IANA zone, nullable), `language` (nullable), `locale` (BCP-47 formatting locale, nullable), and `conversation_style` (`ai_default` / `concise_direct` / `friendly_chatty`, always present). Not a real `Credential` row; never redacted; unredacted in `credentials/README.md`. Scripts access it via the standard dict-comprehension: `creds["current_user"]`. |
| **User's Details** | The free-text `KEY = value` content a user types in **Settings → My profile → User's Details**. Parsed/normalized into the `custom_details` block inside the `current_user` credentials.json record. Stored as `details_raw` (verbatim, for editor re-open) and `details_parsed` (`{UPPER_SNAKE: "value"}` map) on the `User` row. Editing triggers a fan-out re-sync to all running environments of all agents the user owns via `event_user_details_updated`. The same fan-out is triggered when the user updates any locale or communication preference (`timezone`, `language`, `locale`, `conversation_style`) from the profile settings, since those fields also ride the `current_user` block. |
| **Environment Critical State** | A persisted flag (`critical_state=true`) on an `AgentEnvironment` that coexists with `status="running"`: the Docker container is up, but a post-start provisioning step (package install, credential sync) failed. Sessions, chat, terminals, and logs remain available; CRON-scheduled runs and manual "Run now" are blocked until the owner resolves the issue and rebuilds. Distinct from `status="error"` (container not running). |
| **Agent Env Action Log** | Append-only table (`agent_env_action_log`) recording every significant environment operation — rebuild, setup, package install, credential sync, cron-skip — with full, untruncated error detail. The data source for the env-card "Show details" modal. Rows are written by the lifecycle and the scheduler; never updated; deleted only via CASCADE. |
| **Building Mode** | Agent environment state for configuration and development |
| **Conversation Mode** | Agent environment state for executing tasks and chat |
| **Agent User** | Default role for new signups — can install, chat, and manage settings; cannot create agents or publish bundles |
| **Agent Developer** | Admin-promoted role — unlocks agent creation, building mode, publishing, and all developer UI |
| **A2A** | Agent-to-Agent protocol for cross-platform agent communication |
| **MCP** | Model Context Protocol - exposes agents as tool servers to external LLM clients |
| **App MCP Server** | Universal MCP endpoint that routes messages to agents by AI classification. As of Phase 5 of the channels & identity unification, a singleton `ServerChannel` (`channel_type="app_mcp"`, the platform's `authenticated` transport) — admin kill switch, `visibility` + grants, per-user agent scope, all resolved by `ChannelPolicyService` like every other channel. Routes over exactly what a caller owns (`ChannelCandidateProvider`) plus their enabled identities (`IdentityCandidateProvider`, opt-in, off by default); no route, assignment, or per-agent toggle exists any more |
| **Router Trigger Prompt** | Short natural-language sentence on an agent (and snapshotted into bundle revisions), one of the two fields (with `example_prompts`) every routing consumer — App MCP, Server Channels, identity Stage 2 — classifies against. Bundle install and apply-update **do** propagate it — `InstallService` copies `revision.router_trigger_prompt` onto the installed agent row in both paths. What Phase 5 of the channels & identity unification removed is propagation into a *route*: there is no `AppAgentRoute` left to auto-populate, so the copied prompt is now itself what makes the install routable. Editable by the owner via the focused `PATCH /agents/{id}/router-trigger-prompt` (no developer gate) or the generic agent update |
| **Identity Routing** | Person-level routing candidate available to **every** surface, not just App MCP — users expose themselves as a named identity that other users can address by name, resolved by two-stage routing (Stage 1 picks the person, Stage 2 picks one of their agents; `match_method` is `only_one` or `ai`). `IdentityCandidateProvider` supplies the candidates and each surface composes them into its own ballot: App MCP Stage 1, and (since Phase 3 of the channels & identity unification) Server Channels Pass 1, gated there on the sender's own `allow_identity_routing` opt-in. The session opens in the identity owner's workspace either way. |
| **Identity Agent Binding** | Configuration linking one of a user's agents to their identity, with a trigger prompt and per-caller access control |
| **Identity Binding Assignment** | Per-caller access record granting a specific user access to a specific identity agent binding. Its two booleans point in **opposite directions** and are routinely confused: `is_active` is the identity *owner's* control over who may reach them, while `is_enabled` is the *caller's* own per-person opt-out of reaching that owner (the row is keyed by `target_user_id`, and `toggle_identity_contact` filters on it). One person-level `is_enabled` toggle governs every surface — App MCP and Server Channels alike — deliberately not duplicated per channel |
| **MCP Connector** | Configuration that connects an agent environment to an external MCP server |
| **Agent Webapp** | Lightweight data dashboard (HTML/CSS/JS) served from an agent's workspace via shareable URLs |
| **Webapp Share** | Token-based public access to an agent's webapp for unauthenticated viewers |
| **Agent REST API** | A capability-narrowed HTTP API that a producer agent builds from plain Python functions decorated with the `cinna_api` SDK and exposes through the platform proxy. The powerful upstream credential stays in the producer container; consumers get only the surface the producer chose to expose. A deterministic, no-LLM-at-call-time code-to-code channel (contrast with A2A and MCP). |
| **`agent_api` credential** | A `CredentialType.AGENT_API` credential storing `{ base_url, token, spec_url, label, producer_agent_id }` — it **is** the connection between a consumer agent and a producer's REST API. Created by the "Connect Agent API" helper, it rides the existing credential sync / whitelist / redaction pipeline; `token` is redacted in agent prompts. Deleting it disconnects (cascade-deletes the token). Cross-user sharing is safe because the shared value is the narrowed proxy, not the upstream secret. |
| **`agent_api_token`** | Opaque scoped token (SHA256 hash at rest, 8-char display prefix) that authenticates consumer calls to a producer agent's REST API. Internal machine credential — never created or revoked by hand: it is minted by the connect helper and bound to its `agent_api` credential via `credential_id` (`ON DELETE CASCADE`), so deleting the credential is the only way to revoke. Never expires. `read_only_override` may only narrow the producer's `policy.yaml`, never widen it. |
| **`owner_identity_token` (agent_api caller identity)** | A synthetic, host-computed `credentials.json` entry (`id="owner_identity"`, `type="owner_identity_token"`) injected into a consumer env that has ≥1 `agent_api` connection — a sibling of the `current_user` record. It carries a narrow, audience-restricted (`aud="agent_api_caller"`) JWT the agent sends on Agent REST API calls; the proxy verifies it, resolves the *install owner*, **strips** it, and injects trusted `X-Cinna-Caller-*` headers so the producer learns who is calling. Anonymous-by-default (absent/expired ⇒ no headers, never an error); never reaches the producer. Not a real `Credential`; never redacted; re-minted on every credential sync. |
| **Agent REST API access grant** | An `agent_api_access_grant` row, one per `(producer_agent, user)`, by which a producer's owner assigns **capability scopes** to individual platform users from the **Access & Scopes** card (gated by the `agent_api_identity_enabled` opt-in flag). The proxy resolves the grant **live** on every call and injects `X-Cinna-Caller-Scopes` (space-separated); editing scopes takes effect on the next call. The producer is the primary enforcer (`caller.scopes` in the `cinna_api` SDK); the platform optionally hard-denies scope-gated endpoints declared in `policy.yaml` at the edge. |
| **Agent REST API — connection vs. key** | The two products behind one `agent_api` proxy, discriminated by `AgentApiToken.kind`. A **connection** (`kind="connection"`) wires one platform agent to another: anonymous by default, machine-only token, never shown, synced into the consumer's `credentials.json`. A **key** (`kind="external"`, see below) hands a human-operated caller outside the platform the same access. Both ride the identical proxy, `policy.yaml`, and live scope-grant machinery, so a producer's authorization code (`caller.user_id`, `caller.has_scope(...)`) is the same for either caller — one authorization path, not two. |
| **`agent_api` external key** | A hand-issued `agent_api_token` (`kind="external"`) that lets a caller outside the platform — a laptop script, server, or cron job — call a producer agent's REST API as if it were a peer agent. Bound at mint time to a `subject_user_id` (immutable — reassigning identity is revoke + re-issue, never an edit) and optionally an expiry; minting is gated by the producer's `agent_api_external_access_enabled` opt-in, which also acts as a live kill switch on every call. Revealable (once, then again on demand) only via `POST /credentials/{id}/agent-api-key/reveal`, which audits every reveal (`AGENT_API_EXTERNAL_KEY_REVEALED`); the generic `with-data` credential-detail endpoint strips the token for a key instead of showing it. Never synced into `credentials.json`, never shareable (`allow_sharing` forced off), and categorized under **My Credentials** rather than Automatic. Scopes live on the same `agent_api_access_grant` row a connection's caller would use — the key answers *who*, the grant answers *what*. |
| **Agent-to-Agent MCP Connector** | A connector flagged `is_agent_to_agent=true` on `mcp_connector` that a producer agent owner creates to expose their agent as a peer MCP server for other *agents* (rather than external LLM clients). Managed from the Integrations → MCP Connectors card's dedicated sub-tab. Reuses the full MCP connector infrastructure (token verifier, server registry, direct tokens) unchanged; the flag drives producer sub-tab grouping and the consumer picker discoverability. Requires `agent-developer` role to create. |
| **`mcp_provider` credential** | A `CredentialType.MCP_PROVIDER` credential that **is** the connection between a consumer agent's SDK and a remote MCP server — either another platform agent's agent-to-agent connector (`auth_mode="agent2agent"`) or an arbitrary external MCP server (`fixed_token` / `oauth_dcr` / `none`). Created by the "Connect MCP Provider" helper; rides the credential sync / whitelist / redaction / sharing pipeline. Unlike every other credential type, it is **never written to `credentials.json`**: the token is injected into the SDK runtime config (OpenCode `"mcp"` block / Claude Code `options.mcp_servers`) via a per-mode MCP manifest (`user_mcp.json`). Deleting it disconnects and cascade-deletes the bound direct token (agent2agent only). |
| **Account CLI Token** | A `CLIToken` row with `token_type="cli-account"` and `agent_id=NULL`. Created by exchanging an account setup token. Scoped only to the `/account/*` route group — cannot authenticate per-agent sync, exec, workspace, or building-context routes. Carries a 7-day rolling expiry. Stored in `.cinna/account.json` on the developer's machine. |
| **Child Token (CLI)** | A standard `token_type="cli"` `CLIToken` minted programmatically by `POST /account/agents/{id}/mint`. Carries `minted_by_account_token_id` as provenance. Authenticates the unchanged per-agent sync/exec endpoints; revoked individually or by cascade when its parent account token is revoked. |
| **System Notification** | A platform-generated alert about a backend event (e.g. a session error), dispatched to the event owner via email. Distinct from agent-authored messages and the in-app activity feed. Governed by the Notification Catalog and per-user preferences. |
| **Notification Catalog** | The code-only registry (in `notification_catalog.py`) of every system notification type the platform supports. Each entry defines user-facing copy, the default opt-in state, the email template filename, the subject builder, and the dedup key. Adding a new type requires only a new enum value, a catalog entry, and a built email template — no schema migration. |
| **App Sync** | Zero-knowledge server-side sync substrate for native clients (Cinna Desktop, future Cinna Mobile). Stores E2E-encrypted documents partitioned by `collection`; the server never holds a key that can decrypt them. |
| **App Sync Record** | One row in `app_sync_record` representing a single synced entity (a note, a job, a folder, etc.). Keyed by `(user_id, collection, client_entity_id)`; carries an opaque ciphertext payload and a per-user monotonic `seq` for delta pulls. |
| **User Master Key (UMK)** | A per-account 256-bit random key that is the root of the App Sync key hierarchy. Generated once on the first device; never transmitted or stored in plaintext anywhere — the server holds only wrapped (encrypted) copies in `app_sync_key_envelope`. |
| **Key Envelope** | A row in `app_sync_key_envelope` containing the UMK sealed/wrapped under one unlock method (`device`, `recovery`, or `passphrase`). The server stores and returns the `wrapped_key` verbatim and cannot open it. |
| **Skin** | A registered visual overlay (`SkinDefinition` in `frontend/src/config/themes.ts`) selected independently of light/dark mode, applied via `data-skin="…"` on `<html>`. Pure CSS-variable overlay across three tiers — shadcn semantic tokens, Tailwind palette scales, decorative `--skin-*` tokens — with no component-level opt-in. Client-side only (`localStorage`), no backend model. |
| **Device Pairing** | Mechanism for sharing the UMK with a new device via a hardened commit-then-reveal handshake. The joining device generates an ephemeral keypair and a commitment (`blake2b(pubkey ‖ nonce_J)`) and calls `POST /pairing/start`. A trusted unlocked device discovers the request from `GET /pairing/inbox`, posts its `sealer_nonce` via `POST /pairing/inbox/{id}/sealer-nonce`, and seals the UMK via `POST /pairing/inbox/{id}/complete` after the joiner reveals its nonce and the commitment is verified client-side. A 6-digit SAS (`trunc6(blake2b(pubkey ‖ nonce_J ‖ nonce_S))`) is compared out-of-band. The server is a dumb relay: it stores and forwards opaque strings and never verifies the commitment or decrypts the UMK. |
| **Server Channel** | Admin-configured, server-wide inbound transport instance (`ServerChannel`, e.g. Google Chat, Email) that lets people outside the platform message agents. A `webhook` transport (Google Chat) is reached through an unguessable webhook URL; a `polled` transport (Email, since Phase 4 of the channels & identity unification) has no URL at all and is instead pulled by a scheduler — see `ChannelCapabilities.inbound_mode`. Owns its own transport trust model (non-secret `config` + Fernet-encrypted, write-only `encrypted_secrets`, or — for a transport whose credential lives elsewhere, like email's referenced SMTP server — `needs_outbound_credentials=False`), a fail-closed comma-separated email whitelist, and an `auto_register_users` toggle. Superuser-only CRUD via the Channels tab on `/admin/server-configuration`. |
| **Channel Thread Binding** | A `ChannelThreadBinding` row pinning one external channel thread (`thread_key`) to one platform `(user, agent, session)` triple — the conversation state of Server Channels. Created before the session exists (during auto-install) so parked messages have somewhere to land; `pending_install` → `active`, and `failed` self-heals by deletion + re-routing on the next inbound message. A thread belongs to exactly one person — a different sender posting into a bound thread, or the loser of a binding-creation race, is declined rather than routed. |
| **Channel Visibility & Grant** | Two of `ServerChannel`'s four admin-owned availability defaults. `visibility` is `"public"` (every platform user may use the channel) or `"restricted"` (only users with a `server_channel_user_grant` row); a public channel needs no grant rows and never gets any — their absence there means the question was never asked, not that nobody is allowed. Being granted is access, not enablement; both are resolved together by `ChannelPolicyService`. |
| **Channel User Setting** | A `channel_user_setting` row: one person's optional, per-channel override of `ServerChannel`'s admin defaults (`is_enabled`, `agent_scope`, `pinned_agent_id`, `allow_identity_routing`). Absence of the row means "the channel default applies" — it is created lazily, on the user's first edit in Settings → Channels, never at channel-create or account-create time (auto-registered senders and later-created users would otherwise never get one). Resolved against the channel's defaults by `ChannelPolicyService`, the sole place the inherit rules live; `allow_identity_routing` is the one field that never inherits (`NOT NULL DEFAULT false`, no channel-level default): it is the **sender's own** consent that a message of theirs may be routed into somebody else's workspace, where they can read it — never the receiver's gate. It governs **channel and App MCP** identity routing only: External A2A identity access is authorized by its own mechanism (`ExternalAccessPolicy.require_identity_access` plus the owner's binding assignments) and is **not** closed by this switch. Read on every message since Phase 3 of the channels & identity unification (gating the identity half of the Pass-1 ballot, and re-checked before ingesting on an already-bound identity thread), surfaced in Settings → Channels, and audited on transition via `SERVER_CHANNEL_IDENTITY_ROUTING_CHANGED` (severity `medium`). |
| **Channel Agent Scope** | The `agent_scope` resolved per user per channel — `"all"` (every owned agent is a routing candidate, the default), `"list"` (only the agents on the user's `channel_user_agent` list), or `"none"` (no agent routes until the user opts one in). An out-of-scope owned agent is recorded in the routing trace as a skip (`SKIP_NOT_IN_CHANNEL_SCOPE`) rather than silently absent, and routing Pass 2 (catalog auto-install) does not run unless the resolved scope is `"all"` — a decided product semantic, not `allow_auto_install` re-read. |
| **Pinned Channel Agent** | A user's `channel_user_setting.pinned_agent_id` — "always route my messages on this channel to this one agent of mine." Skips classification entirely, records its own routing-trace `match_method="pinned"`, outranks both the `only_one` short-circuit and Pass 2 auto-install (a pinned channel never auto-installs, even when the pin fails to resolve — the sender's instruction stands whether or not the platform can honour it), and has its ownership re-checked at resolution time since the foreign key only guarantees the agent still exists, never that this user still owns it. |
| **Server Auto-Install List** | The server-wide `ServerAutoInstallBundle` list of catalog bundles eligible for Pass-2 channel routing — classified by router trigger prompt only when none of a sender's own installed agents match. List membership is never an implicit permission grant; `CatalogService.user_can_install` still gates every candidate per user. |
| **Routing Decision / Routing Trace** | A `RoutingDecision` row — the durable, superuser-only record of one LLM-mediated routing choice: every candidate considered including rejected ones with a `skip_reason`, the rendered classifier prompt, raw model response, provider/model attempts, verdict, latency, and (message-text-gated) the sender's own words. Every row also carries the **`origin`** that produced it — `server_channel` / `email` / `app_mcp` / `simulate`, with `identity` reserved and unemitted because identity routing is a *stage* inside another origin's decision — and a **`channel_id`**, which since Phase 6 of the channels & identity unification is populated on every live surface (App MCP is a singleton `ServerChannel`, so it has one too). Captured in memory by `RoutingTrace` at the router chokepoint, persisted once the decision settles. See [Auto Routing Tuning](application/routing_tuning/routing_tuning.md). |
| **Reachability Verdict** | A backend-computed, plain-language sentence (one of **16** codes, each with a matching test) explaining why a routing decision went the way it did and what to change. Split by origin for *wording only*, never for the underlying finding: as of Phase 5 of the channels & identity unification, a channel decision and an App MCP decision are diagnosed by the identical logic — ownership and wording (e.g. an owned agent unreachable because it has neither a router trigger prompt nor example prompts) — differing only in the wording each origin gets. Phase 6 of the channels & identity unification replaced the two-way channel-or-App-MCP split with an origin→profile map of **five** remedy profiles (`routing_reachability_service.py`) — `channel`, `email` (the channel profile under its own name), `app_mcp`, an origin-neutral `generic` default, and a reserved `identity` — so an origin the map does not know degrades to origin-neutral wording rather than silently inheriting App MCP's. The six App-MCP-only route-based codes this entry used to describe (e.g. a standalone agent unreachable because it has no `AppAgentRoute`) are deleted outright, not merged, since the route mechanism they diagnosed no longer exists. The headline output of [Auto Routing Tuning](application/routing_tuning/routing_tuning.md). |
| **Transitional Status** | A status value meaning "a background operation currently owns this row and will clear it when it finishes" — `AgentEnvironment.status` in `creating`/`building`/`rebuilding`/`starting`/`activating`, `Session.interaction_status` in `running`/`pending_stream`, `InputTask.status="in_progress"`, `ChannelTurnDelivery.role="draft"`. Set before a long fire-and-forget operation and normally cleared on completion; a backend restart or crash mid-operation (`asyncio.CancelledError`, a `BaseException` that skips every `except Exception` fallback) can abandon a row in this state forever, invisible to every status-filtered scheduler and blocking the user's own way out (e.g. rebuild is refused while an environment is transitional). Reconciled by the Status Repair Sweep, below. |
| **Status Repair Sweep** | A `BackgroundScheduler` cron (`STATUS_REPAIR_*` settings in `config.py`, 2-minute default interval, single-leader via Postgres advisory lock) that reconciles rows abandoned in a Transitional Status across four domains, in one ordered sweep per tick: environments → sessions → input tasks → channel turn deliveries. Verifies live evidence (container probe, session/relay state) before writing — never repairs on age alone — and relies on `AgentEnvironment.status_changed_at` as a liveness heartbeat, stamped unconditionally by the lifecycle's sole status writers (`_set_status`/`_touch_progress`), to tell a slow-but-alive operation from a dead one. See [status_repair.md](system/status_repair/status_repair.md). |

---

## Domain Map

| Domain | Description |
|--------|-------------|
| [agents](#agents) | Core agent lifecycle - creation, configuration, environments, sessions, chat, file management |
| [tasks](#tasks) | Task submission, refinement, triggers, and scheduling |
| [credentials](#credentials) | Credential management, encryption, AI provider keys |
| [application](#application) | User-facing platform features - authentication, roles, integrations, real-time events, workspaces |
| [knowledge](#knowledge) | Git-based knowledge sources, vector search, RAG |
| [sharing](#sharing) | Guest access, workspace collaboration |
| [agentic_teams](#agentic_teams) | Visual org-chart builder for agent orchestration topology — teams, nodes, connections with AI-generated handover prompts |
| [system](#system) | Cross-domain backend infrastructure — background reconciliation that spans multiple domains rather than owning one |
| [development](#development) | Backend/frontend patterns, AI functions, debugging |
| [infrastructure](#infrastructure) | Deployment-level concerns — nginx reverse proxy, well-known URIs, origin-root routing |
| [flows](#flows) | Cross-cutting mechanism narratives that follow one concern end to end across several features — message ingress, auth tokens, credential materialization, routing and identity |

---

## Wiki

| Article | Description |
|---------|-------------|
| [Why Cinna Agents?](wiki/skills_plugins_vs_cinna_agents.md) | Skills & plugins vs. Cinna agents — point-by-point comparison for SMBs evaluating AI automation approaches |
| [Claude Managed Agents vs. Cinna](wiki/claude_managed_agents_vs_cinna_agents.md) | Claude Managed Agents (Anthropic's hosted agent API) vs. Cinna agents — architecture, capabilities, and trade-offs |

---

## Feature Registry

Generated from the frontmatter of each feature's primary doc — edit the frontmatter, then run `make docs-registry`. Tooling: `docs/development/docs_tooling.md`.

<!-- registry:start -->
<!-- Generated by .cinna-core-kit/scripts/docs_index.py registry — do not edit by hand.
     Edit the frontmatter of each feature's primary doc instead. -->

### agents

| Feature | Description | Docs | Related |
|---------|-------------|------|---------|
| agent_addons | Gives an owner and any viewer one merged list of everything an agent carries beyond its prompt, folding plugins and skills that used to live on two separate tabs into one. | [business logic](agents/agent_addons/agent_addons.md) \| [tech](agents/agent_addons/agent_addons_tech.md) | [agent_bundles](agents/agent_bundles/agent_bundles.md), [agent_environment_core](agents/agent_environment_core/agent_environment_core.md), [agent_plugins](agents/agent_plugins/agent_plugins.md), [agent_prompts](agents/agent_prompts/agent_prompts.md), [agent_skills](agents/agent_skills/agent_skills.md), [cinna_cli_integration](application/cinna_cli_integration/cinna_cli_integration.md), +3 more |
| agent_api | Lets a producer agent expose a capability-narrowed REST API from its own code, so other agents or outside callers can use it as a normal credentialed API without touching its underlying secrets. | [business logic](agents/agent_api/agent_api.md) \| [tech](agents/agent_api/agent_api_tech.md) \| [spec viewer](agents/agent_api/spec_viewer.md) \| [spec viewer tech](agents/agent_api/spec_viewer_tech.md) | [a2a_access_tokens](application/a2a_integration/a2a_access_tokens/a2a_access_tokens.md), [account_cli_workspace](application/cinna_cli_integration/account_cli_workspace.md), [agent_bundles](agents/agent_bundles/agent_bundles.md), [agent_credentials](agents/agent_credentials/agent_credentials.md), [agent_environment_core](agents/agent_environment_core/agent_environment_core.md), [agent_environment_data_management](agents/agent_environment_data_management/agent_environment_data_management.md), +5 more |
| agent_app_data | Gives each user a persistent, private per-bundle storage volume that survives uninstall and reinstall, with manual wipe of orphaned copies and background cleanup once the account or install is gone. | [business logic](agents/agent_app_data/agent_app_data.md) \| [tech](agents/agent_app_data/agent_app_data_tech.md) | [agent_bundles](agents/agent_bundles/agent_bundles.md), [agent_environment_data_management](agents/agent_environment_data_management/agent_environment_data_management.md), [agent_environments](agents/agent_environments/agent_environments.md), [agent_management](application/agent_management/agent_management.md), [agent_status_tracking](agents/agent_status_tracking/agent_status_tracking.md), [user_roles](application/user_roles/user_roles.md) |
| agent_bundles | Lets a publisher ship an agent as a versioned bundle that other users install, update, and manage independently, with app-style visibility, grants, and automatic or manual update convergence. | [business logic](agents/agent_bundles/agent_bundles.md) \| [tech](agents/agent_bundles/agent_bundles_tech.md) | [account_cli_workspace](application/cinna_cli_integration/account_cli_workspace.md), [admin_agent_environments](application/admin_agent_environments/admin_agent_environments.md), [admin_ai_credential_provisioning](application/ai_credentials/admin_ai_credential_provisioning.md), [agent_addons](agents/agent_addons/agent_addons.md), [agent_api](agents/agent_api/agent_api.md), [agent_app_data](agents/agent_app_data/agent_app_data.md), +24 more |
| agent_commands | Lets a user run instant, deterministic slash commands during an agent session without an LLM call, with an autocomplete popup that also lists invocable skills. | [business logic](agents/agent_commands/agent_commands.md) \| [tech](agents/agent_commands/agent_commands_tech.md) \| [files](agents/agent_commands/files_command.md) \| [recovery](agents/agent_commands/session_recovery_command.md) \| [reset](agents/agent_commands/session_reset_command.md) \| [rebuild-env](agents/agent_commands/rebuild_env_command.md) \| [webapp](agents/agent_commands/webapp_command.md) \| [autocomplete](agents/agent_commands/slash_command_autocomplete.md) \| [non-llm context bridging tech](agents/agent_commands/non_llm_context_bridging_tech.md) | [a2a_protocol](application/a2a_integration/a2a_protocol/a2a_protocol.md), [agent_environments](agents/agent_environments/agent_environments.md), [agent_file_management](agents/agent_file_management/agent_file_management.md), [agent_improvement_requests](application/agent_improvement_requests/agent_improvement_requests.md), [agent_sessions](application/agent_sessions/agent_sessions.md), [agent_skills](agents/agent_skills/agent_skills.md), +5 more |
| agent_credentials | Syncs an owner's credentials into their agent environments with whitelisting and redaction, and lets them share, template, or blast-radius-gate delete credentials across users and published bundles. | [business logic](agents/agent_credentials/agent_credentials.md) \| [tech](agents/agent_credentials/agent_credentials_tech.md) \| [oauth](agents/agent_credentials/oauth_credentials.md) \| [whitelist](agents/agent_credentials/credentials_whitelist.md) \| [whitelist tech](agents/agent_credentials/credentials_whitelist_tech.md) \| [google SA](agents/agent_credentials/google_service_account.md) \| [google SA tech](agents/agent_credentials/google_service_account_tech.md) \| [ssh key](agents/agent_credentials/ssh_key_credentials.md) \| [ssh key tech](agents/agent_credentials/ssh_key_credentials_tech.md) \| [sharing](agents/agent_credentials/credential_sharing.md) \| [sharing tech](agents/agent_credentials/credential_sharing_tech.md) \| [security hardening](agents/agent_credentials/credential_security_hardening.md) \| [security hardening tech](agents/agent_credentials/credential_security_hardening_tech.md) \| [add credential widget](agents/agent_credentials/add_credential_widget.md) | [admin_ai_credential_provisioning](application/ai_credentials/admin_ai_credential_provisioning.md), [agent_api](agents/agent_api/agent_api.md), [agent_bundles](agents/agent_bundles/agent_bundles.md), [agent_environment_core](agents/agent_environment_core/agent_environment_core.md), [agent_environment_data_management](agents/agent_environment_data_management/agent_environment_data_management.md), [agent_environments](agents/agent_environments/agent_environments.md), +17 more |
| agent_env_console | Lets an owner or developer tail an environment's live container logs and open a full interactive terminal inside it, both in a right-side drawer, with terminal use fully audited. | [business logic](agents/agent_environments/agent_env_console.md) \| [tech](agents/agent_environments/agent_env_console_tech.md) | [admin_agent_environments](application/admin_agent_environments/admin_agent_environments.md), [agent_environment_core](agents/agent_environment_core/agent_environment_core.md), [agent_environments](agents/agent_environments/agent_environments.md), [cinna_cli_integration](application/cinna_cli_integration/cinna_cli_integration.md), [nginx_setup](infrastructure/nginx_setup.md), [user_roles](application/user_roles/user_roles.md) |
| agent_env_critical_state | Flags a running-but-degraded environment for owner attention when provisioning fails after the container starts, pausing CRON runs and notifying the owner without disrupting chat or terminal access. | [business logic](agents/agent_environments/agent_env_critical_state.md) \| [tech](agents/agent_environments/agent_env_critical_state_tech.md) | [agent_environments](agents/agent_environments/agent_environments.md), [agent_schedulers](agents/agent_schedulers/agent_schedulers.md), [model_freshness](agents/agent_environments/model_freshness.md), [realtime_events](application/realtime_events/event_bus_system.md), [status_repair](system/status_repair/status_repair.md), [system_notifications](application/system_notifications/system_notifications.md) |
| agent_environment_core | Runs the server-side core inside every agent's Docker container — HTTP API, prompt generation, and adapters for the Claude Code and OpenCode SDK engines across multiple LLM providers. | [business logic](agents/agent_environment_core/agent_environment_core.md) \| [tech](agents/agent_environment_core/agent_environment_core_tech.md) \| [multi-sdk](agents/agent_environment_core/multi_sdk.md) \| [multi-sdk tech](agents/agent_environment_core/multi_sdk_tech.md) \| [knowledge tool](agents/agent_environment_core/knowledge_tool.md) \| [create agent task tool](agents/agent_environment_core/create_agent_task_tool.md) \| [session state tools](agents/agent_environment_core/session_state_tools.md) \| [opencode interactive questions](agents/agent_environment_core/opencode_interactive_questions.md) \| [opencode interactive questions tech](agents/agent_environment_core/opencode_interactive_questions_tech.md) \| [claude code interactive tools](agents/agent_environment_core/claude_code_interactive_tools.md) \| [claude code interactive tools tech](agents/agent_environment_core/claude_code_interactive_tools_tech.md) | [account_cli_workspace](application/cinna_cli_integration/account_cli_workspace.md), [admin_agent_environments](application/admin_agent_environments/admin_agent_environments.md), [agent_addons](agents/agent_addons/agent_addons.md), [agent_api](agents/agent_api/agent_api.md), [agent_credentials](agents/agent_credentials/agent_credentials.md), [agent_env_console](agents/agent_environments/agent_env_console.md), +24 more |
| agent_environment_data_management | Defines which workspace data is bundle-owned, per-user App Data, credentials, or runtime data, and keeps that split consistent across install, apply-update, and environment migration. | [business logic](agents/agent_environment_data_management/agent_environment_data_management.md) \| [tech](agents/agent_environment_data_management/agent_environment_data_management_tech.md) | [agent_api](agents/agent_api/agent_api.md), [agent_app_data](agents/agent_app_data/agent_app_data.md), [agent_bundles](agents/agent_bundles/agent_bundles.md), [agent_credentials](agents/agent_credentials/agent_credentials.md), [agent_environment_core](agents/agent_environment_core/agent_environment_core.md), [agent_environments](agents/agent_environments/agent_environments.md), +8 more |
| agent_environments | Provides the isolated Docker container runtime each agent executes in, split into a system layer and a user-workspace layer so platform updates never destroy user work. | [business logic](agents/agent_environments/agent_environments.md) \| [tech](agents/agent_environments/agent_environments_tech.md) \| [multi-image](agents/agent_environments/agent_multi_image_environments.md) \| [credential rebuild](agents/agent_environments/affected_environments_rebuild.md) \| [sdk session persistence](agents/agent_environments/sdk_session_persistence.md) | [a2a_protocol](application/a2a_integration/a2a_protocol/a2a_protocol.md), [admin_agent_environments](application/admin_agent_environments/admin_agent_environments.md), [agent_activities](application/agent_activities/agent_activities.md), [agent_app_data](agents/agent_app_data/agent_app_data.md), [agent_bundles](agents/agent_bundles/agent_bundles.md), [agent_commands](agents/agent_commands/agent_commands.md), +21 more |
| agent_file_management | Lets users upload, browse, and download files in an agent's workspace and chat history, under a per-user storage quota with daily garbage collection of deleted files. | [business logic](agents/agent_file_management/agent_file_management.md) \| [tech](agents/agent_file_management/agent_file_management_tech.md) \| [remote db viewer](agents/agent_file_management/remote_database_viewer.md) \| [remote db viewer tech](agents/agent_file_management/remote_database_viewer_tech.md) | [agent_commands](agents/agent_commands/agent_commands.md), [agent_environment_core](agents/agent_environment_core/agent_environment_core.md), [agent_environments](agents/agent_environments/agent_environments.md), [agent_message_attachments](agents/agent_file_management/agent_message_attachments.md), [agent_sessions](application/agent_sessions/agent_sessions.md), [chat_windows](application/chat_interface/chat_windows.md), +3 more |
| agent_git_versioning | Version-controls an agent's workspace in an external git repo: checkout, connect, push, and pull with conflict resolution, plus a webhook for automatic pulls. | [business logic](agents/agent_git_versioning/agent_git_versioning.md) \| [tech](agents/agent_git_versioning/agent_git_versioning_tech.md) | [agent_bundles](agents/agent_bundles/agent_bundles.md), [agent_credentials](agents/agent_credentials/agent_credentials.md), [agent_environment_data_management](agents/agent_environment_data_management/agent_environment_data_management.md), [agent_management](application/agent_management/agent_management.md), [agent_skills](agents/agent_skills/agent_skills.md), [agent_webhooks](agents/agent_webhooks/agent_webhooks.md), +5 more |
| agent_handover | Lets a conversational agent delegate work to another agent directly, or create an inbox task for the user to route manually, using AI-assisted prompt generation. | [business logic](agents/agent_handover/agent_handover.md) \| [tech](agents/agent_handover/agent_handover_tech.md) | [agent_activities](application/agent_activities/agent_activities.md), [agent_bundles](agents/agent_bundles/agent_bundles.md), [agent_environment_core](agents/agent_environment_core/agent_environment_core.md), [agent_management](application/agent_management/agent_management.md), [agent_prompts](agents/agent_prompts/agent_prompts.md), [agent_sessions](application/agent_sessions/agent_sessions.md), +5 more |
| agent_message_attachments | Lets an agent attach files from its workspace to its own reply messages, rendered as inline preview cards and delivered to external clients via signed download links. | [business logic](agents/agent_file_management/agent_message_attachments.md) \| [tech](agents/agent_file_management/agent_message_attachments_tech.md) | [a2a_protocol](application/a2a_integration/a2a_protocol/a2a_protocol.md), [agent_environment_core](agents/agent_environment_core/agent_environment_core.md), [agent_file_management](agents/agent_file_management/agent_file_management.md), [agent_sessions](application/agent_sessions/agent_sessions.md), [chat_windows](application/chat_interface/chat_windows.md), [external_agent_access](application/external_agent_access/external_agent_access.md), +4 more |
| agent_plugins | Installs and manages plugins for an agent from git marketplaces, bundles, or the skills catalog, syncing manifest-driven files into the container with no backend cache. | [business logic](agents/agent_plugins/agent_plugins.md) \| [tech](agents/agent_plugins/agent_plugins_tech.md) | [agent_addons](agents/agent_addons/agent_addons.md), [agent_bundles](agents/agent_bundles/agent_bundles.md), [agent_credentials](agents/agent_credentials/agent_credentials.md), [agent_environment_core](agents/agent_environment_core/agent_environment_core.md), [agent_environment_data_management](agents/agent_environment_data_management/agent_environment_data_management.md), [agent_management](application/agent_management/agent_management.md), +7 more |
| agent_prompts | Assembles an agent's building- and conversation-mode system prompts and keeps the editable prompt documents synced between the database and the environment. | [business logic](agents/agent_prompts/agent_prompts.md) \| [tech](agents/agent_prompts/agent_prompts_tech.md) | [a2a_protocol](application/a2a_integration/a2a_protocol/a2a_protocol.md), [account_cli_workspace](application/cinna_cli_integration/account_cli_workspace.md), [agent_addons](agents/agent_addons/agent_addons.md), [agent_bundles](agents/agent_bundles/agent_bundles.md), [agent_credentials](agents/agent_credentials/agent_credentials.md), [agent_environment_core](agents/agent_environment_core/agent_environment_core.md), +16 more |
| agent_schedulers | Lets a user configure multiple named cron-style schedules per agent from natural language, each firing a prompt session or a conditional script trigger. | [business logic](agents/agent_schedulers/agent_schedulers.md) \| [tech](agents/agent_schedulers/agent_schedulers_tech.md) | [account_cli_workspace](application/cinna_cli_integration/account_cli_workspace.md), [agent_bundles](agents/agent_bundles/agent_bundles.md), [agent_env_critical_state](agents/agent_environments/agent_env_critical_state.md), [agent_environments](agents/agent_environments/agent_environments.md), [agent_management](application/agent_management/agent_management.md), [agent_prompts](agents/agent_prompts/agent_prompts.md), +6 more |
| agent_skills | Lets an agent carry reusable skill folders that the engine loads on demand, publishable to and installable from a server-wide skills catalog. | [business logic](agents/agent_skills/agent_skills.md) \| [tech](agents/agent_skills/agent_skills_tech.md) | [account_cli_workspace](application/cinna_cli_integration/account_cli_workspace.md), [agent_addons](agents/agent_addons/agent_addons.md), [agent_bundles](agents/agent_bundles/agent_bundles.md), [agent_commands](agents/agent_commands/agent_commands.md), [agent_environment_core](agents/agent_environment_core/agent_environment_core.md), [agent_environment_data_management](agents/agent_environment_data_management/agent_environment_data_management.md), +6 more |
| agent_status_tracking | Lets an agent self-report a lightweight health status that the platform caches and surfaces on cards, REST, a slash command, and A2A. | [business logic](agents/agent_status_tracking/agent_status_tracking.md) \| [tech](agents/agent_status_tracking/agent_status_tracking_tech.md) \| [/agent-status command](agents/agent_commands/agent_status_command.md) | [a2a_protocol](application/a2a_integration/a2a_protocol/a2a_protocol.md), [account_cli_workspace](application/cinna_cli_integration/account_cli_workspace.md), [agent_activities](application/agent_activities/agent_activities.md), [agent_app_data](agents/agent_app_data/agent_app_data.md), [agent_commands](agents/agent_commands/agent_commands.md), [agent_environment_core](agents/agent_environment_core/agent_environment_core.md), +5 more |
| agent_webapp | Lets an agent serve a lightweight HTML/JS dashboard from its workspace over a shareable URL, backed by dynamic Python data endpoints. | [business logic](agents/agent_webapp/agent_webapp.md) \| [tech](agents/agent_webapp/agent_webapp_tech.md) \| [chat widget](agents/agent_webapp/webapp_chat.md) \| [chat tech](agents/agent_webapp/webapp_chat_tech.md) \| [chat context](agents/agent_webapp/webapp_chat_context.md) \| [chat context tech](agents/agent_webapp/webapp_chat_context_tech.md) \| [chat actions](agents/agent_webapp/webapp_chat_actions.md) \| [chat actions tech](agents/agent_webapp/webapp_chat_actions_tech.md) \| [actions context](agents/agent_webapp/webapp_actions_context.md) \| [actions context tech](agents/agent_webapp/webapp_actions_context_tech.md) | [agent_api](agents/agent_api/agent_api.md), [agent_commands](agents/agent_commands/agent_commands.md), [agent_environment_core](agents/agent_environment_core/agent_environment_core.md), [agent_environment_data_management](agents/agent_environment_data_management/agent_environment_data_management.md), [agent_environments](agents/agent_environments/agent_environments.md), [agent_prompts](agents/agent_prompts/agent_prompts.md), +6 more |
| agent_webhooks | Lets an external system trigger an agent over an authenticated HTTP webhook, either starting a session or running a script, with invocation logging. | [business logic](agents/agent_webhooks/agent_webhooks.md) \| [tech](agents/agent_webhooks/agent_webhooks_tech.md) | [agent_environment_core](agents/agent_environment_core/agent_environment_core.md), [agent_git_versioning](agents/agent_git_versioning/agent_git_versioning.md), [agent_management](application/agent_management/agent_management.md), [agent_schedulers](agents/agent_schedulers/agent_schedulers.md), [agent_sessions](application/agent_sessions/agent_sessions.md), [flow_auth_token_taxonomy](flows/auth_token_taxonomy.md), +4 more |
| cli_commands | Lets an agent declare named shell commands in its workspace that users and A2A clients can run directly, bypassing the LLM. | [business logic](agents/cli_commands/cli_commands.md) \| [tech](agents/cli_commands/cli_commands_tech.md) | [a2a_protocol](application/a2a_integration/a2a_protocol/a2a_protocol.md), [agent_commands](agents/agent_commands/agent_commands.md), [agent_sessions](application/agent_sessions/agent_sessions.md), [agent_skills](agents/agent_skills/agent_skills.md), [agent_status_tracking](agents/agent_status_tracking/agent_status_tracking.md), [cinna_mcp_descriptor](application/external_agent_access/cinna_mcp_descriptor.md), +1 more |
| model_freshness | Detects when an environment's configured LLM model is deprecated or unavailable and nudges the owner with an amber badge and email, never auto-upgrading. | [business logic](agents/agent_environments/model_freshness.md) \| [tech](agents/agent_environments/model_freshness_tech.md) | [admin_agent_environments](application/admin_agent_environments/admin_agent_environments.md), [admin_ai_credential_provisioning](application/ai_credentials/admin_ai_credential_provisioning.md), [agent_bundles](agents/agent_bundles/agent_bundles.md), [agent_env_critical_state](agents/agent_environments/agent_env_critical_state.md), [agent_environment_core](agents/agent_environment_core/agent_environment_core.md), [agent_environments](agents/agent_environments/agent_environments.md), +3 more |

### tasks

| Feature | Description | Docs | Related |
|---------|-------------|------|---------|
| input_tasks | Lets a user or agent submit, AI-refine, execute, and track a task through comments, attachments, status history, and team subtask delegation. | [business logic](application/input_tasks/input_tasks.md) \| [tech](application/input_tasks/input_tasks_tech.md) | [agent_activities](application/agent_activities/agent_activities.md), [agent_environment_core](agents/agent_environment_core/agent_environment_core.md), [agent_file_management](agents/agent_file_management/agent_file_management.md), [agent_handover](agents/agent_handover/agent_handover.md), [agent_management](application/agent_management/agent_management.md), [agent_prompts](agents/agent_prompts/agent_prompts.md), +13 more |
| task_triggers | Lets a task fire automatically on a CRON schedule, a one-time date, or an inbound webhook, appending a payload to the task description. | [business logic](application/input_tasks/task_triggers.md) \| [tech](application/input_tasks/task_triggers_tech.md) | [agent_schedulers](agents/agent_schedulers/agent_schedulers.md), [agent_webhooks](agents/agent_webhooks/agent_webhooks.md), [flow_message_ingress](flows/message_ingress.md), [input_tasks](application/input_tasks/input_tasks.md), [nginx_setup](infrastructure/nginx_setup.md) |
| tools_approval | Gates tools a plugin introduces behind explicit one-time user approval, stored per agent and merged with the always-allowed core SDK tools. | [business logic](agents/agent_environment_core/tools_approval_management.md) \| [tech](agents/agent_environment_core/tools_approval_management_tech.md) | [agent_credentials](agents/agent_credentials/agent_credentials.md), [agent_environment_core](agents/agent_environment_core/agent_environment_core.md), [agent_management](application/agent_management/agent_management.md), [agent_plugins](agents/agent_plugins/agent_plugins.md), [agent_sessions](application/agent_sessions/agent_sessions.md), [chat_windows](application/chat_interface/chat_windows.md) |

### credentials

| Feature | Description | Docs | Related |
|---------|-------------|------|---------|
| admin_ai_credential_provisioning | Superuser connects an AI provider (shared or per-user-minted key) that auto-provisions and reconciles per-user AI credentials for chosen roles. | [business logic](application/ai_credentials/admin_ai_credential_provisioning.md) \| [tech](application/ai_credentials/admin_ai_credential_provisioning_tech.md) | [agent_bundles](agents/agent_bundles/agent_bundles.md), [agent_credentials](agents/agent_credentials/agent_credentials.md), [ai_credentials](application/ai_credentials/ai_credentials.md), [auth](application/auth/auth.md), [desktop_auth](application/desktop_auth/desktop_auth.md), [external_agent_access](application/external_agent_access/external_agent_access.md), +5 more |
| ai_credentials | Stores encrypted per-user LLM provider API keys as named credentials with prioritized default resolution, environment linking, and sharing between users. | [business logic](application/ai_credentials/ai_credentials.md) \| [tech](application/ai_credentials/ai_credentials_tech.md) \| [provider adapters tech](application/ai_credentials/provider_adapters_tech.md) \| [anthropic types](application/ai_credentials/anthropic_credential_types.md) \| [anthropic types tech](application/ai_credentials/anthropic_credential_types_tech.md) \| [affected envs](application/ai_credentials/affected_environments_widget.md) \| [ai functions routing](application/ai_credentials/ai_functions_sdk_routing.md) | [admin_ai_credential_provisioning](application/ai_credentials/admin_ai_credential_provisioning.md), [agent_bundles](agents/agent_bundles/agent_bundles.md), [agent_environment_core](agents/agent_environment_core/agent_environment_core.md), [agent_environment_data_management](agents/agent_environment_data_management/agent_environment_data_management.md), [agent_environments](agents/agent_environments/agent_environments.md), [agent_management](application/agent_management/agent_management.md), +7 more |

### application

| Feature | Description | Docs | Related |
|---------|-------------|------|---------|
| a2a_access_tokens | Scoped JWT tokens that let an external A2A client access one agent's conversation or building mode without full user credentials. | [business logic](application/a2a_integration/a2a_access_tokens/a2a_access_tokens.md) \| [tech](application/a2a_integration/a2a_access_tokens/a2a_access_tokens_tech.md) | [a2a_protocol](application/a2a_integration/a2a_protocol/a2a_protocol.md), [agent_api](agents/agent_api/agent_api.md), [agent_management](application/agent_management/agent_management.md), [agent_sessions](application/agent_sessions/agent_sessions.md), [flow_auth_token_taxonomy](flows/auth_token_taxonomy.md), [flow_message_ingress](flows/message_ingress.md), +1 more |
| a2a_protocol | Exposes platform agents as standard A2A protocol endpoints so external agents and A2A-compatible tools can discover and message them. | [business logic](application/a2a_integration/a2a_protocol/a2a_protocol.md) \| [tech](application/a2a_integration/a2a_protocol/a2a_protocol_tech.md) \| [v1 support](application/a2a_integration/a2a_protocol/a2a_v1_support.md) | [a2a_access_tokens](application/a2a_integration/a2a_access_tokens/a2a_access_tokens.md), [agent_commands](agents/agent_commands/agent_commands.md), [agent_environments](agents/agent_environments/agent_environments.md), [agent_management](application/agent_management/agent_management.md), [agent_message_attachments](agents/agent_file_management/agent_message_attachments.md), [agent_prompts](agents/agent_prompts/agent_prompts.md), +8 more |
| account_cli_workspace | Lets a developer bootstrap one account-wide CLI workspace that discovers, builds, and manages all their agents without per-agent setup. | [business logic](application/cinna_cli_integration/account_cli_workspace.md) \| [tech](application/cinna_cli_integration/account_cli_workspace_tech.md) | [account_device_login](application/cinna_cli_integration/account_device_login.md), [agent_api](agents/agent_api/agent_api.md), [agent_bundles](agents/agent_bundles/agent_bundles.md), [agent_environment_core](agents/agent_environment_core/agent_environment_core.md), [agent_improvement_requests](application/agent_improvement_requests/agent_improvement_requests.md), [agent_prompts](agents/agent_prompts/agent_prompts.md), +13 more |
| account_device_login | Lets a developer refresh an expired account CLI token by approving a device-login code in the browser instead of pasting a new setup token. | [business logic](application/cinna_cli_integration/account_device_login.md) \| [tech](application/cinna_cli_integration/account_device_login_tech.md) | [account_cli_workspace](application/cinna_cli_integration/account_cli_workspace.md), [flow_auth_token_taxonomy](flows/auth_token_taxonomy.md) |
| admin_agent_environments | Superuser console that lists every fleet environment's staleness, model health, and bundle update status, with targeted and bulk rebuild. | [business logic](application/admin_agent_environments/admin_agent_environments.md) \| [tech](application/admin_agent_environments/admin_agent_environments_tech.md) | [agent_bundles](agents/agent_bundles/agent_bundles.md), [agent_env_console](agents/agent_environments/agent_env_console.md), [agent_environment_core](agents/agent_environment_core/agent_environment_core.md), [agent_environments](agents/agent_environments/agent_environments.md), [model_freshness](agents/agent_environments/model_freshness.md), [realtime_events](application/realtime_events/event_bus_system.md), +1 more |
| agent_activities | Persistent per-user activity feed and sidebar bell that surface session, task, and error events, including ones missed while disconnected. | [business logic](application/agent_activities/agent_activities.md) \| [tech](application/agent_activities/agent_activities_tech.md) | [agent_environments](agents/agent_environments/agent_environments.md), [agent_handover](agents/agent_handover/agent_handover.md), [agent_management](application/agent_management/agent_management.md), [agent_sessions](application/agent_sessions/agent_sessions.md), [agent_status_tracking](agents/agent_status_tracking/agent_status_tracking.md), [email_integration](application/email_integration/email_integration.md), +6 more |
| agent_improvement_requests | Lets a session owner consent-share a frozen transcript and runtime context with the agent's publisher so they can fix what went wrong. | [business logic](application/agent_improvement_requests/agent_improvement_requests.md) \| [tech](application/agent_improvement_requests/agent_improvement_requests_tech.md) | [account_cli_workspace](application/cinna_cli_integration/account_cli_workspace.md), [agent_bundles](agents/agent_bundles/agent_bundles.md), [agent_commands](agents/agent_commands/agent_commands.md), [agent_credentials](agents/agent_credentials/agent_credentials.md), [agent_environments](agents/agent_environments/agent_environments.md), [agent_sessions](application/agent_sessions/agent_sessions.md), +1 more |
| agent_management | Owns an agent's persistent definition — identity, prompts, SDK, credentials, and integrations — the config entry point every platform feature builds on. | [business logic](application/agent_management/agent_management.md) \| [creation wizard](application/agent_management/new_agent_wizard.md) | [a2a_access_tokens](application/a2a_integration/a2a_access_tokens/a2a_access_tokens.md), [a2a_protocol](application/a2a_integration/a2a_protocol/a2a_protocol.md), [agent_activities](application/agent_activities/agent_activities.md), [agent_app_data](agents/agent_app_data/agent_app_data.md), [agent_bundles](agents/agent_bundles/agent_bundles.md), [agent_credentials](agents/agent_credentials/agent_credentials.md), +24 more |
| agent_sessions | Persistent chat conversations between users or external systems and an agent's environment, with streaming responses and lifecycle tracking. | [business logic](application/agent_sessions/agent_sessions.md) \| [tech](application/agent_sessions/agent_sessions_tech.md) \| [env panel widget](application/agent_sessions/app_env_panel_widget.md) \| [channel ingestion](application/agent_sessions/channel_ingestion.md) \| [channel ingestion tech](application/agent_sessions/channel_ingestion_tech.md) \| [ask user question widget](application/agent_sessions/tool_answer_questions_widget.md) | [a2a_access_tokens](application/a2a_integration/a2a_access_tokens/a2a_access_tokens.md), [a2a_protocol](application/a2a_integration/a2a_protocol/a2a_protocol.md), [agent_activities](application/agent_activities/agent_activities.md), [agent_commands](agents/agent_commands/agent_commands.md), [agent_credentials](agents/agent_credentials/agent_credentials.md), [agent_environment_core](agents/agent_environment_core/agent_environment_core.md), +29 more |
| agent_to_agent_mcp_connector | Lets an agent connect to another platform agent, or any external server, as a live MCP tool provider injected directly into its SDK runtime. | [business logic](application/mcp_integration/agent_to_agent_mcp_connector.md) \| [tech](application/mcp_integration/agent_to_agent_mcp_connector_tech.md) | [account_cli_workspace](application/cinna_cli_integration/account_cli_workspace.md), [agent_api](agents/agent_api/agent_api.md), [agent_credentials](agents/agent_credentials/agent_credentials.md), [agent_environment_core](agents/agent_environment_core/agent_environment_core.md), [agent_plugins](agents/agent_plugins/agent_plugins.md), [flow_auth_token_taxonomy](flows/auth_token_taxonomy.md), +2 more |
| app_mcp_server | One universal MCP endpoint per user that AI-classifies each message to the right agent or identity contact and streams back its reply. | [business logic](application/app_mcp_server/app_mcp_server.md) \| [tech](application/app_mcp_server/app_mcp_server_tech.md) \| [prompt examples](application/app_mcp_server/prompt_examples.md) \| [prompt examples tech](application/app_mcp_server/prompt_examples_tech.md) | [agent_bundles](agents/agent_bundles/agent_bundles.md), [agent_management](application/agent_management/agent_management.md), [agent_prompts](agents/agent_prompts/agent_prompts.md), [agent_sessions](application/agent_sessions/agent_sessions.md), [ai_functions](development/backend/ai_functions_development.md), [external_agent_access](application/external_agent_access/external_agent_access.md), +8 more |
| app_sync | Syncs a native client's personal data (notes, jobs, folders) across devices as opaque end-to-end encrypted ciphertext the server can never read. | [business logic](application/app_sync/app_sync.md) \| [tech](application/app_sync/app_sync_tech.md) | [agent_prompts](agents/agent_prompts/agent_prompts.md), [agent_sessions](application/agent_sessions/agent_sessions.md), [desktop_auth](application/desktop_auth/desktop_auth.md), [external_agent_access](application/external_agent_access/external_agent_access.md), [input_tasks](application/input_tasks/input_tasks.md), [nginx_setup](infrastructure/nginx_setup.md) |
| auth | Authenticates users via JWT, password login, or Google OAuth, with password recovery, invitations, and an admin-configured registration and sign-in policy. | [business logic](application/auth/auth.md) \| [tech](application/auth/auth_tech.md) \| [google oauth](application/auth/google_oauth.md) \| [google oauth tech](application/auth/google_oauth_tech.md) | [admin_ai_credential_provisioning](application/ai_credentials/admin_ai_credential_provisioning.md), [ai_credentials](application/ai_credentials/ai_credentials.md), [desktop_auth](application/desktop_auth/desktop_auth.md), [desktop_onboarding](application/desktop_onboarding/desktop_onboarding.md), [email_confirmation](application/auth/email_confirmation.md), [flow_auth_token_taxonomy](flows/auth_token_taxonomy.md), +8 more |
| chat_windows | Renders streaming agent chat — markdown, tool blocks, auto-scroll — across session pages, guest shares, webapp widgets, and dashboard prompt actions. | [business logic](application/chat_interface/chat_windows.md) \| [tech](application/chat_interface/chat_windows_tech.md) \| [tool rendering](application/chat_interface/tool_rendering.md) \| [tool tech](application/chat_interface/tool_rendering_tech.md) \| [markdown](application/chat_interface/markdown_rendering.md) \| [markdown tech](application/chat_interface/markdown_rendering_tech.md) \| [auto-scroll](application/chat_interface/auto_scroll_and_streaming_display.md) \| [auto-scroll tech](application/chat_interface/auto_scroll_and_streaming_display_tech.md) \| [ask user question](application/chat_interface/tool_answer_questions_widget.md) \| [tool approval](application/chat_interface/tool_approval_widget.md) \| [tool approval tech](application/chat_interface/tool_approval_widget_tech.md) \| [webapp widget](application/chat_interface/webapp_chat_widget.md) \| [webapp widget tech](application/chat_interface/webapp_chat_widget_tech.md) \| [file sending](application/chat_interface/file_sending_and_ui.md) \| [file sending tech](application/chat_interface/file_sending_and_ui_tech.md) \| [dashboard prompt actions](application/chat_interface/dashboard_prompt_actions.md) \| [dashboard prompt actions tech](application/chat_interface/dashboard_prompt_actions_tech.md) | [agent_commands](agents/agent_commands/agent_commands.md), [agent_environment_core](agents/agent_environment_core/agent_environment_core.md), [agent_file_management](agents/agent_file_management/agent_file_management.md), [agent_handover](agents/agent_handover/agent_handover.md), [agent_message_attachments](agents/agent_file_management/agent_message_attachments.md), [agent_sessions](application/agent_sessions/agent_sessions.md), +10 more |
| cinna_cli_integration | Local development CLI that syncs a user's editor with a remote agent workspace in real time, so agents can be built with local tools while credentials stay on the platform. | [business logic](application/cinna_cli_integration/cinna_cli_integration.md) \| [tech](application/cinna_cli_integration/cinna_cli_integration_tech.md) \| [local dev](application/cinna_cli_integration/local_cli_development.md) | [account_cli_workspace](application/cinna_cli_integration/account_cli_workspace.md), [agent_addons](agents/agent_addons/agent_addons.md), [agent_credentials](agents/agent_credentials/agent_credentials.md), [agent_env_console](agents/agent_environments/agent_env_console.md), [agent_environment_core](agents/agent_environment_core/agent_environment_core.md), [agent_environment_data_management](agents/agent_environment_data_management/agent_environment_data_management.md), +9 more |
| cinna_mcp_descriptor | Lets Cinna Desktop present any reachable agent as an emulated MCP tool, using a descriptor carried on the agent's A2A card and discovery entry. | [business logic](application/external_agent_access/cinna_mcp_descriptor.md) \| [tech](application/external_agent_access/cinna_mcp_descriptor_tech.md) | [a2a_protocol](application/a2a_integration/a2a_protocol/a2a_protocol.md), [cli_commands](agents/cli_commands/cli_commands.md), [desktop_auth](application/desktop_auth/desktop_auth.md), [external_agent_access](application/external_agent_access/external_agent_access.md), [mcp_integration](application/mcp_integration/agent_mcp_architecture.md) |
| desktop_auth | OAuth 2.0 with PKCE authentication for Cinna Desktop and Cinna Mobile, covering token rotation, per-device revocation, and a consent-free grant linked from an existing CLI account token. | [business logic](application/desktop_auth/desktop_auth.md) \| [tech](application/desktop_auth/desktop_auth_tech.md) | [account_cli_workspace](application/cinna_cli_integration/account_cli_workspace.md), [admin_ai_credential_provisioning](application/ai_credentials/admin_ai_credential_provisioning.md), [app_sync](application/app_sync/app_sync.md), [auth](application/auth/auth.md), [cinna_mcp_descriptor](application/external_agent_access/cinna_mcp_descriptor.md), [desktop_onboarding](application/desktop_onboarding/desktop_onboarding.md), +7 more |
| desktop_onboarding | Lets a newly created user get Cinna Desktop installed, connected, and signed in with one click, without a terminal or an invite token. | [business logic](application/desktop_onboarding/desktop_onboarding.md) \| [tech](application/desktop_onboarding/desktop_onboarding_tech.md) | [account_cli_workspace](application/cinna_cli_integration/account_cli_workspace.md), [auth](application/auth/auth.md), [cinna_cli_integration](application/cinna_cli_integration/cinna_cli_integration.md), [desktop_auth](application/desktop_auth/desktop_auth.md), [flow_auth_token_taxonomy](flows/auth_token_taxonomy.md), [local_agent_kit](application/local_agent_kit/local_agent_kit.md), +4 more |
| email_confirmation | Requires a user to confirm their email address before the platform sends outbound mail on their behalf, and ties agent-creation limits to that status. | [business logic](application/auth/email_confirmation.md) \| [tech](application/auth/email_confirmation_tech.md) | [account_cli_workspace](application/cinna_cli_integration/account_cli_workspace.md), [agent_bundles](agents/agent_bundles/agent_bundles.md), [auth](application/auth/auth.md), [email_integration](application/email_integration/email_integration.md), [flow_auth_token_taxonomy](flows/auth_token_taxonomy.md), [system_notifications](application/system_notifications/system_notifications.md), +1 more |
| email_integration | Routes inbound and outbound mail for a shared organizational mailbox as one of the Server Channels transports, alongside Google Chat. | [business logic](application/email_integration/email_integration.md) \| [tech](application/email_integration/email_integration_tech.md) | [agent_activities](application/agent_activities/agent_activities.md), [agent_bundles](agents/agent_bundles/agent_bundles.md), [agent_credentials](agents/agent_credentials/agent_credentials.md), [agent_management](application/agent_management/agent_management.md), [agent_sessions](application/agent_sessions/agent_sessions.md), [agent_status_tracking](agents/agent_status_tracking/agent_status_tracking.md), +8 more |
| email_sessions | Keeps email conversations routed through Server Channels threaded correctly and delivers replies through a durable, retrying outbound queue. | [business logic](application/email_integration/email_sessions.md) \| [tech](application/email_integration/email_sessions_tech.md) | [agent_activities](application/agent_activities/agent_activities.md), [agent_sessions](application/agent_sessions/agent_sessions.md), [email_integration](application/email_integration/email_integration.md), [flow_message_ingress](flows/message_ingress.md), [input_tasks](application/input_tasks/input_tasks.md), [mail_servers](application/email_integration/mail_servers.md), +1 more |
| external_agent_access | REST and A2A surface that lets native clients like Cinna Desktop discover a user's own agents and identity contacts and chat with them without the web app. | [business logic](application/external_agent_access/external_agent_access.md) | [a2a_protocol](application/a2a_integration/a2a_protocol/a2a_protocol.md), [admin_ai_credential_provisioning](application/ai_credentials/admin_ai_credential_provisioning.md), [agent_bundles](agents/agent_bundles/agent_bundles.md), [agent_message_attachments](agents/agent_file_management/agent_message_attachments.md), [agent_sessions](application/agent_sessions/agent_sessions.md), [app_mcp_server](application/app_mcp_server/app_mcp_server.md), +6 more |
| getting_started | Guides new users and new instances through initial setup: configuring an AI credential, learning core concepts, and discovering supplementary features. | [business logic](application/getting_started/getting_started.md) \| [tech](application/getting_started/getting_started_tech.md) | [ai_credentials](application/ai_credentials/ai_credentials.md), [knowledge_sources](application/knowledge_sources/knowledge_sources.md), [local_agent_kit](application/local_agent_kit/local_agent_kit.md), [plugin_marketplaces](application/plugin_marketplaces/plugin_marketplaces.md), [server_configuration](application/server_configuration/server_configuration.md), [user_workspaces](application/user_workspaces/user_workspaces.md) |
| identity_routing | Lets a user expose themselves as a routable identity so callers on App MCP or Server Channels can address them by name and reach one of their agents. | [business logic](application/identity_routing/identity_routing.md) \| [tech](application/identity_routing/identity_routing_tech.md) | [agent_management](application/agent_management/agent_management.md), [agent_sessions](application/agent_sessions/agent_sessions.md), [app_mcp_server](application/app_mcp_server/app_mcp_server.md), [external_agent_access](application/external_agent_access/external_agent_access.md), [flow_message_ingress](flows/message_ingress.md), [flow_routing_identity_chain](flows/routing_identity_chain.md), +4 more |
| knowledge_sources | Lets admins connect Git repositories of documentation that agents search semantically during sessions, public sources available to every user's agents. | [business logic](application/knowledge_sources/knowledge_sources.md) \| [tech](application/knowledge_sources/knowledge_sources_tech.md) | [agent_environment_core](agents/agent_environment_core/agent_environment_core.md), [agent_environment_data_management](agents/agent_environment_data_management/agent_environment_data_management.md), [agent_environments](agents/agent_environments/agent_environments.md), [agent_git_versioning](agents/agent_git_versioning/agent_git_versioning.md), [agent_management](application/agent_management/agent_management.md), [agent_prompts](agents/agent_prompts/agent_prompts.md), +4 more |
| local_agent_kit | Gives anyone without a Cinna account a versioned starter kit for building local AI agents in a layout compatible with a future cloud agent workspace. | [business logic](application/local_agent_kit/local_agent_kit.md) \| [tech](application/local_agent_kit/local_agent_kit_tech.md) | [account_cli_workspace](application/cinna_cli_integration/account_cli_workspace.md), [agent_addons](agents/agent_addons/agent_addons.md), [agent_bundles](agents/agent_bundles/agent_bundles.md), [agent_prompts](agents/agent_prompts/agent_prompts.md), [agent_skills](agents/agent_skills/agent_skills.md), [cinna_cli_integration](application/cinna_cli_integration/cinna_cli_integration.md), +4 more |
| mail_servers | Lets admins configure reusable, server-scoped IMAP and SMTP connections that email channels reference for receiving and sending mail. | [business logic](application/email_integration/mail_servers.md) \| [tech](application/email_integration/mail_servers_tech.md) | [email_integration](application/email_integration/email_integration.md), [email_sessions](application/email_integration/email_sessions.md), [flow_message_ingress](flows/message_ingress.md), [server_channels](application/server_channels/server_channels.md), [server_configuration](application/server_configuration/server_configuration.md), [system_notifications](application/system_notifications/system_notifications.md) |
| mcp_integration | Exposes platform agents as remote MCP servers with OAuth 2.1 and per-user access control, so any MCP-compatible client can connect to and use them as tools. | [architecture](application/mcp_integration/agent_mcp_architecture.md) \| [implementation](application/mcp_integration/agent_mcp_connector.md) \| [setup](application/mcp_integration/mcp_connector_setup.md) | [a2a_protocol](application/a2a_integration/a2a_protocol/a2a_protocol.md), [agent_management](application/agent_management/agent_management.md), [agent_sessions](application/agent_sessions/agent_sessions.md), [agent_to_agent_mcp_connector](application/mcp_integration/agent_to_agent_mcp_connector.md), [app_mcp_server](application/app_mcp_server/app_mcp_server.md), [auth](application/auth/auth.md), +5 more |
| plugin_marketplaces | Lets admins register Git-based marketplaces of plugins and skills that users install into their agents, syncing metadata into Postgres with no persistent file cache. | [business logic](application/plugin_marketplaces/plugin_marketplaces.md) \| [tech](application/plugin_marketplaces/plugin_marketplaces_tech.md) | [agent_addons](agents/agent_addons/agent_addons.md), [agent_plugins](agents/agent_plugins/agent_plugins.md), [getting_started](application/getting_started/getting_started.md), [ssh_keys](application/ssh_keys/ssh_keys.md) |
| realtime_events | Backend-to-frontend and backend-to-backend event bus that broadcasts real-time updates over WebSockets and drives server-side event handlers. | [event bus](application/realtime_events/event_bus_system.md) \| [streaming](application/realtime_events/frontend_backend_agentenv_streaming.md) | [admin_agent_environments](application/admin_agent_environments/admin_agent_environments.md), [agent_activities](application/agent_activities/agent_activities.md), [agent_api](agents/agent_api/agent_api.md), [agent_commands](agents/agent_commands/agent_commands.md), [agent_env_critical_state](agents/agent_environments/agent_env_critical_state.md), [agent_environment_core](agents/agent_environment_core/agent_environment_core.md), +16 more |
| routing_tuning | Gives superusers durable, replayable traces of every LLM-mediated routing decision so they can see why a channel, email, or App MCP message did or didn't reach an agent. | [business logic](application/routing_tuning/routing_tuning.md) \| [tech](application/routing_tuning/routing_tuning_tech.md) | [agent_activities](application/agent_activities/agent_activities.md), [agent_bundles](agents/agent_bundles/agent_bundles.md), [agent_prompts](agents/agent_prompts/agent_prompts.md), [ai_functions](development/backend/ai_functions_development.md), [app_mcp_server](application/app_mcp_server/app_mcp_server.md), [flow_message_ingress](flows/message_ingress.md), +3 more |
| server_channels | Lets people outside the platform, such as Google Chat or email users, message platform agents through admin-configured, whitelisted inbound channels. | [business logic](application/server_channels/server_channels.md) \| [tech](application/server_channels/server_channels_tech.md) \| [debug monitor](application/server_channels/channel_debug_monitor.md) \| [debug monitor tech](application/server_channels/channel_debug_monitor_tech.md) | [agent_activities](application/agent_activities/agent_activities.md), [agent_bundles](agents/agent_bundles/agent_bundles.md), [agent_file_management](agents/agent_file_management/agent_file_management.md), [agent_git_versioning](agents/agent_git_versioning/agent_git_versioning.md), [agent_management](application/agent_management/agent_management.md), [agent_message_attachments](agents/agent_file_management/agent_message_attachments.md), +17 more |
| server_configuration | Admin-configurable server-wide settings for the login disclaimer, registration and sign-in policy, the public landing page, channels, and mail servers. | [business logic](application/server_configuration/server_configuration.md) \| [disclaimer](application/server_configuration/disclaimer.md) \| [disclaimer tech](application/server_configuration/disclaimer_tech.md) \| [access policy](application/server_configuration/access_policy.md) \| [access policy tech](application/server_configuration/access_policy_tech.md) \| [landing page](application/server_configuration/landing_page.md) \| [landing page tech](application/server_configuration/landing_page_tech.md) | [admin_agent_environments](application/admin_agent_environments/admin_agent_environments.md), [admin_ai_credential_provisioning](application/ai_credentials/admin_ai_credential_provisioning.md), [auth](application/auth/auth.md), [backend_patterns](development/backend/backend_development_llm.md), [desktop_auth](application/desktop_auth/desktop_auth.md), [desktop_onboarding](application/desktop_onboarding/desktop_onboarding.md), +7 more |
| ssh_keys | Lets users store SSH key pairs so the platform can clone and sync private Git repositories on their behalf. | [business logic](application/ssh_keys/ssh_keys.md) \| [tech](application/ssh_keys/ssh_keys_tech.md) | [agent_credentials](agents/agent_credentials/agent_credentials.md), [agent_git_versioning](agents/agent_git_versioning/agent_git_versioning.md), [knowledge_sources](application/knowledge_sources/knowledge_sources.md), [plugin_marketplaces](application/plugin_marketplaces/plugin_marketplaces.md) |
| system_notifications | Generic platform notification layer that dispatches typed, throttled alerts like session errors to users based on their own notification preferences. | [business logic](application/system_notifications/system_notifications.md) \| [tech](application/system_notifications/system_notifications_tech.md) | [agent_activities](application/agent_activities/agent_activities.md), [agent_env_critical_state](agents/agent_environments/agent_env_critical_state.md), [agent_plugins](agents/agent_plugins/agent_plugins.md), [agent_sessions](application/agent_sessions/agent_sessions.md), [ai_credentials](application/ai_credentials/ai_credentials.md), [auth](application/auth/auth.md), +5 more |
| theming | Lets a user personalize color mode and visual skin for their own device, applying instantly across the app with no backend model. | [business logic](application/theming/theming.md) \| [tech](application/theming/theming_tech.md) | [chat_windows](application/chat_interface/chat_windows.md), [frontend_patterns](development/frontend/frontend_development_llm.md) |
| user_2fa | Lets a user enroll a passkey or TOTP app as a required second login step, with recovery codes and per-browser trusted-device skip. | [business logic](application/user_2fa/user_2fa.md) \| [tech](application/user_2fa/user_2fa_tech.md) | [a2a_access_tokens](application/a2a_integration/a2a_access_tokens/a2a_access_tokens.md), [auth](application/auth/auth.md), [desktop_auth](application/desktop_auth/desktop_auth.md), [flow_auth_token_taxonomy](flows/auth_token_taxonomy.md) |
| user_dashboards | Lets a user build a personal grid of monitoring blocks for their agents, showing webapp views, session summaries, or task lists across workspaces. | [business logic](application/user_dashboards/user_dashboards.md) \| [tech](application/user_dashboards/user_dashboards_tech.md) | [chat_windows](application/chat_interface/chat_windows.md) |
| user_roles | Gates agent-building features behind a three-value role so most users just use agents while a smaller group of developers builds and publishes them. | [business logic](application/user_roles/user_roles.md) \| [tech](application/user_roles/user_roles_tech.md) | [account_cli_workspace](application/cinna_cli_integration/account_cli_workspace.md), [admin_ai_credential_provisioning](application/ai_credentials/admin_ai_credential_provisioning.md), [agent_addons](agents/agent_addons/agent_addons.md), [agent_app_data](agents/agent_app_data/agent_app_data.md), [agent_bundles](agents/agent_bundles/agent_bundles.md), [agent_credentials](agents/agent_credentials/agent_credentials.md), +8 more |
| user_workspaces | Lets a user split their agents, credentials, sessions, and tasks into separate named contexts to reduce clutter, with workspace filtering optional per user. | [business logic](application/user_workspaces/user_workspaces.md) \| [tech](application/user_workspaces/user_workspaces_tech.md) | [account_cli_workspace](application/cinna_cli_integration/account_cli_workspace.md), [agent_credentials](agents/agent_credentials/agent_credentials.md), [agent_management](application/agent_management/agent_management.md), [auth](application/auth/auth.md), [getting_started](application/getting_started/getting_started.md), [knowledge_sources](application/knowledge_sources/knowledge_sources.md), +1 more |

### knowledge

| Feature | Description | Docs | Related |
|---------|-------------|------|---------|
| knowledge_sources | Lets admins connect Git repositories of documentation that agents search semantically during sessions, public sources available to every user's agents. | [business logic](application/knowledge_sources/knowledge_sources.md) \| [tech](application/knowledge_sources/knowledge_sources_tech.md) | [agent_environment_core](agents/agent_environment_core/agent_environment_core.md), [agent_environment_data_management](agents/agent_environment_data_management/agent_environment_data_management.md), [agent_environments](agents/agent_environments/agent_environments.md), [agent_git_versioning](agents/agent_git_versioning/agent_git_versioning.md), [agent_management](application/agent_management/agent_management.md), [agent_prompts](agents/agent_prompts/agent_prompts.md), +4 more |

### sharing

| Feature | Description | Docs | Related |
|---------|-------------|------|---------|
| guest_sharing | Lets an agent owner create disposable, optionally code-protected links so anonymous visitors can chat with the owner's live agent without an account. | [business logic](agents/guest_sharing/guest_sharing.md) \| [tech](agents/guest_sharing/guest_sharing_tech.md) | [agent_credentials](agents/agent_credentials/agent_credentials.md), [agent_file_management](agents/agent_file_management/agent_file_management.md), [agent_management](application/agent_management/agent_management.md), [agent_message_attachments](agents/agent_file_management/agent_message_attachments.md), [agent_sessions](application/agent_sessions/agent_sessions.md), [agent_webapp](agents/agent_webapp/agent_webapp.md), +4 more |
| user_workspaces | Lets a user split their agents, credentials, sessions, and tasks into separate named contexts to reduce clutter, with workspace filtering optional per user. | [business logic](application/user_workspaces/user_workspaces.md) \| [tech](application/user_workspaces/user_workspaces_tech.md) | [account_cli_workspace](application/cinna_cli_integration/account_cli_workspace.md), [agent_credentials](agents/agent_credentials/agent_credentials.md), [agent_management](application/agent_management/agent_management.md), [auth](application/auth/auth.md), [getting_started](application/getting_started/getting_started.md), [knowledge_sources](application/knowledge_sources/knowledge_sources.md), +1 more |

### agentic_teams

| Feature | Description | Docs | Related |
|---------|-------------|------|---------|
| agentic_teams | Lets a user build a visual org chart of their own agents with directed handover connections, currently a static blueprint with no execution. | [business logic](agents/agentic_teams/agentic_teams.md) \| [tech](agents/agentic_teams/agentic_teams_tech.md) | [account_cli_workspace](application/cinna_cli_integration/account_cli_workspace.md), [agent_handover](agents/agent_handover/agent_handover.md), [input_tasks](application/input_tasks/input_tasks.md) |

### system

| Feature | Description | Docs | Related |
|---------|-------------|------|---------|
| status_repair | Background sweep that finds database rows stuck in a transitional status after a crash and repairs them only once live evidence confirms they are actually dead. | [business logic](system/status_repair/status_repair.md) \| [tech](system/status_repair/status_repair_tech.md) | [admin_ai_credential_provisioning](application/ai_credentials/admin_ai_credential_provisioning.md), [agent_env_critical_state](agents/agent_environments/agent_env_critical_state.md), [agent_environments](agents/agent_environments/agent_environments.md), [agent_sessions](application/agent_sessions/agent_sessions.md), [input_tasks](application/input_tasks/input_tasks.md), [server_channels](application/server_channels/server_channels.md) |

### development

| Feature | Description | Docs | Related |
|---------|-------------|------|---------|
| ai_functions | Reference for building fast LLM-powered utility functions with multi-provider cascade fallback for reliability. | [reference](development/backend/ai_functions_development.md) | [agent_handover](agents/agent_handover/agent_handover.md), [agent_schedulers](agents/agent_schedulers/agent_schedulers.md), [app_mcp_server](application/app_mcp_server/app_mcp_server.md), [routing_tuning](application/routing_tuning/routing_tuning.md), [server_channels](application/server_channels/server_channels.md) |
| backend_patterns | Reference for backend development patterns: SQLModel models, routes, services, CRUD, and migrations. | [reference](development/backend/backend_development_llm.md) \| [development guidelines](development/development_guidelines_llm.md) \| [debug docker sigkill](development/backend/debug_docker_sigkill_events.md) | [server_configuration](application/server_configuration/server_configuration.md) |
| docs_tooling | Frontmatter-driven docs index: generated README registry for humans, summary/search/impact/graph commands for agents, and checks that keep docs tied to code. | [reference](development/docs_tooling.md) |  |
| frontend_patterns | Reference for frontend development patterns: component conventions, hooks, and TanStack Router and Query usage. | [reference](development/frontend/frontend_development_llm.md) | [theming](application/theming/theming.md), [ui_ux_guidelines](development/frontend/ui_ux_guidelines.md) |
| ui_ux_guidelines | Reference for structuring any frontend surface: user stories, placement thresholds, house patterns, anti-patterns, and the review checklist. | [reference](development/frontend/ui_ux_guidelines.md) | [frontend_patterns](development/frontend/frontend_development_llm.md) |
| user_selector_pattern | Reference for the shared user-picker component and search endpoint that every user-sharing surface on the platform reuses. | [reference](development/frontend/user_selector_pattern.md) | [agent_bundles](agents/agent_bundles/agent_bundles.md), [agent_credentials](agents/agent_credentials/agent_credentials.md), [identity_routing](application/identity_routing/identity_routing.md), [mcp_integration](application/mcp_integration/agent_mcp_architecture.md) |

### infrastructure

| Feature | Description | Docs | Related |
|---------|-------------|------|---------|
| nginx_setup | Reference for the nginx location blocks the platform requires in production and in the frontend container, with links to the features that depend on them. | [reference](infrastructure/nginx_setup.md) | [agent_env_console](agents/agent_environments/agent_env_console.md), [agent_webhooks](agents/agent_webhooks/agent_webhooks.md), [app_mcp_server](application/app_mcp_server/app_mcp_server.md), [app_sync](application/app_sync/app_sync.md), [cinna_cli_integration](application/cinna_cli_integration/cinna_cli_integration.md), [desktop_auth](application/desktop_auth/desktop_auth.md), +5 more |

### flows

| Feature | Description | Docs | Related |
|---------|-------------|------|---------|
| flow_auth_token_taxonomy | Follows every token and credential the platform mints — from the function that creates it, through the dependency that trusts it, to the write that kills it. | [flow](flows/auth_token_taxonomy.md) | [a2a_access_tokens](application/a2a_integration/a2a_access_tokens/a2a_access_tokens.md), [a2a_protocol](application/a2a_integration/a2a_protocol/a2a_protocol.md), [account_cli_workspace](application/cinna_cli_integration/account_cli_workspace.md), [account_device_login](application/cinna_cli_integration/account_device_login.md), [agent_api](agents/agent_api/agent_api.md), [agent_credentials](agents/agent_credentials/agent_credentials.md), +15 more |
| flow_credential_materialization | Follows a Credential or AICredential row from creation and sharing to the files, MCP manifest and env vars a running agent container can actually read, and back on every change. | [flow](flows/credential_materialization.md) | [admin_ai_credential_provisioning](application/ai_credentials/admin_ai_credential_provisioning.md), [agent_api](agents/agent_api/agent_api.md), [agent_bundles](agents/agent_bundles/agent_bundles.md), [agent_credentials](agents/agent_credentials/agent_credentials.md), [agent_environment_core](agents/agent_environment_core/agent_environment_core.md), [agent_environments](agents/agent_environments/agent_environments.md), +2 more |
| flow_message_ingress | Follows one inbound message from any entry point — web chat, server channels, A2A, App MCP, webhooks or triggers — into a turn in an agent session and the reply back out. | [flow](flows/message_ingress.md) | [a2a_access_tokens](application/a2a_integration/a2a_access_tokens/a2a_access_tokens.md), [a2a_protocol](application/a2a_integration/a2a_protocol/a2a_protocol.md), [agent_environment_core](agents/agent_environment_core/agent_environment_core.md), [agent_handover](agents/agent_handover/agent_handover.md), [agent_message_attachments](agents/agent_file_management/agent_message_attachments.md), [agent_schedulers](agents/agent_schedulers/agent_schedulers.md), +17 more |
| flow_routing_identity_chain | Follows an attributed inbound message from sender resolution through channel policy, two-pass routing, identity Stage 2 and auto-install, to the agent, session and recorded verdict. | [flow](flows/routing_identity_chain.md) | [agent_bundles](agents/agent_bundles/agent_bundles.md), [agent_prompts](agents/agent_prompts/agent_prompts.md), [agent_sessions](application/agent_sessions/agent_sessions.md), [app_mcp_server](application/app_mcp_server/app_mcp_server.md), [flow_message_ingress](flows/message_ingress.md), [identity_routing](application/identity_routing/identity_routing.md), +2 more |

<!-- registry:end -->

## Architecture Overview

```
User ──→ Frontend (React) ──→ Backend API (FastAPI) ──→ Services ──→ PostgreSQL
                                      │
                                      ├──→ Docker Environments ──→ Agent SDK (Claude/OpenAI)
                                      ├──→ WebSocket (Socket.IO) ──→ Real-time Events
                                      ├──→ A2A Protocol ──→ External Agents
                                      ├──→ MCP Server ──→ External LLM Clients
                                      │       └──→ App MCP Server ──→ Stage 1 Router ──→ Stage 2 Identity Router
                                      └──→ Server Channels (Google Chat webhook, Email IMAP/SMTP poll) ──→ Channel Routing
```

---

*Last updated: 2026-08-26* <!-- channel message attachments: inbound file attachments for server channels -->
