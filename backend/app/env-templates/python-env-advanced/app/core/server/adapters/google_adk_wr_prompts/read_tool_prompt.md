# Read Tool

Reads file contents from the workspace.

**Usage**: Provide a file path (relative or absolute). Relative paths resolve from `CLAUDE_CODE_WORKSPACE`.

**When to use**: Only when you need to examine a file's contents. Do not use for simple questions.

**Example**: `./scripts/my_script.py` or `./docs/README.md`
