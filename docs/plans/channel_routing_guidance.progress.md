# Channel routing guidance — progress checkpoint

Source of truth for scope: `docs/drafts/channel_routing_guidance_brief.md` (no separate plan; planner skipped per team-lead).
Decisions 1–4 confirmed as recommended (templated replies; list ballot on `none`; clarify on Google Chat only; keep `only_one` short-circuit).
Pipeline per phase: developer (opus) → code-reviewer (opus) ∥ test-writer → test-runner (sonnet, topic groups). Docs (sonnet) once at the end across all phases.
No git operations by anyone; the user commits. Unrelated untracked at start: `docs/drafts/channel_routing_guidance_brief.md`.

## Log

- 2026-09-14 — run started (manager). Tree clean except the brief. Phase 1 (classifier contract) developer launched.
- 2026-09-14 — P1 developed (developer a71ae3c7df10f459e). Files: app_agent_router_prompt.md, services/routing/agent_classifier.py, services/routing/routing_trace.py. `intent`/`options` parsed + on trace SAFE_STAGE_FIELDS (options as `{ref_id}`). Gap for P2: `classify()` returns None on NONE ⇒ help/none only visible on trace; needs opt-in path. Reviewer + test-writer launched in parallel.
- 2026-09-14 — P1 review (reviewer a14796c3c4a431ab6): CHANGES REQUESTED, non-blocking. F1 off-ballot agent_id still clarify (fix); F2 help⇒NONE now reaches Stage 2/App MCP — ACCEPTED as brief-intended deviation (document in P4), help rule narrowed to meta only; nits F3–F5 fixed. Phase 2 shape adopted: `AgentClassifier.classify_answer(...) -> ClassifierAnswer(intent|None, result)`, `classify()` delegates; only channel_routing_service Pass 1/Pass 2 switch; intent=None on outage/non-JSON/bad id (stays no_match); help on Pass 1 returns before Pass 2 catalog call; reuse OptionTrace/SAFE_OPTION_FIELDS for guidance_options.
- 2026-09-14 — P1 tests (test-writer abf822de62f371873): 160 passed in 4 files (test_router_quoted_context_prompt, test_agent_classifier_parsing, test_routing_trace, routing_message_text_gating_test). Accepted behaviour: best pick prepended to options and counts toward the two. Fixes relayed to developer by message.
- 2026-09-14 — P1 fixes applied (developer a71ae3c7df10f459e, resumed): F1 off-ballot ⇒ (route, ()); help rule narrowed; F3–F5; prompt now 4842 bytes sha256 17576006…8757. Reviewer resumed on fix delta; test-writer resumed to re-pin + cover F1/F4.
- 2026-09-14 — P1 fixes APPROVED (reviewer a14796c3c4a431ab6). P1 tests updated (test-writer abf822de62f371873): 162 passed / 0 failed in the 4 files. Regression test-runner (sonnet) launched: architecture, unit, api/routing, api/server_channels, identity, app_mcp.
- 2026-09-14 — P1 regression GREEN (test-runner af5e00069af533a75): 2813 passed, 10 skipped, 0 failed in 7:41 (architecture 88, unit 2375, routing 122, server_channels 182, identity 18, app_mcp 38). Runner stopped during a redundant `-rs` re-run (result already in hand). **PHASE 1 DONE.**
- 2026-09-14 — P2 (guidance replies) developer launched. P2 decisions: clarify still routes best pick until P3; `none` + empty ballot + catalog offer lists bundles; settings for all three knobs added in P2; client regenerated for simulate `guidance_reply`.
- 2026-09-14 — P2 developed (developer a097011836160bd26). New `services/server_channels/channel_routing_guidance.py` (GuidanceEntry, RoutingGuidance, build_guidance, compose_guidance_reply); ClassifierAnswer/classify_answer; OUTCOME_GUIDED + guidance_kind/options + note_guidance; BallotOffer + RoutingDecisionResult.guidance + trailing decide params (guidance_enabled default False, max_listed 5); inbound guidance branch + DEBUG_GUIDED; 3 settings; simulate guidance_reply; purity allowlist (+_PLAIN_DATA_ANNOTATIONS); FE client regenerated. markdown_to_chat handles `- `/`1. ` lists. Breaks by design: tests/utils/routing.py `_CLASSIFY_TARGET` must patch classify_answer; `_route_catalog` now returns (bundle, offer). Reviewer (resumed) + test-writer (resumed) launched in parallel.
- 2026-09-14 — P2 review (reviewer a14796c3c4a431ab6): CHANGES REQUESTED, non-blocking. Confirmed settings wired (inbound :2399, simulate :209), entitlement-only listing, safe trace/debug, same settle path, outage ⇒ no-match. Fixes relayed to developer a097011836160bd26: F1 escape `< > |` + strip `[ ] ~` in `_one_line`; F2 `compose_guidance_reply(markdown=)` plain for email; F3 email-looking name ⇒ local part; F4 `RoutingSimulatePublic` subclass + client regen; F6 own catalog skip reason for help. F5 (Pass 1 none + Pass 2 unusable ⇒ REPLY_NO_MATCH) KEPT — document in P4. Test-writer told the fixed behaviour.
- 2026-09-14 — P2 fixes applied (developer a097011836160bd26, resumed): escaping via exported `LINK_LABEL_SUBSTITUTIONS`; `compose_guidance_reply(markdown=)` + `_markdown_supported(channel)`; `_display_name` local part; EXTRA accepted: identity entries use constant `IDENTITY_DESCRIPTION` (trigger prompt carries owner email); `RoutingSimulatePublic` + client regen; `SKIP_PASS_1_GUIDED="pass_1_guided"`. Reviewer resumed on delta; test-writer given exact shapes.
- 2026-09-14 — P2 fixes APPROVED (reviewer a14796c3c4a431ab6). Client regen verified: only `RoutingSimulatePublic` added (+ simulate response type switch), no unrelated drift. Carry to P4 FE: `routingCopy.ts:87` needs `pass_1_guided` label next to the `guided` outcome badge. Waiting on P2 test-writer.
- 2026-09-14 — P2 tests (test-writer abf822de62f371873, context >250K ⇒ use a FRESH test-writer for P3): 337 passed / 0 failed across 17 files. Stubs now patch `classify_answer` (`tests/utils/routing.py`: `classify_no_match` ⇒ intent none, new `classify_unusable`, helpers `classifier_answer`/`unusable_classifier_reply`/`as_classifier_answer`); `tests/utils/server_channel.py::route_installed` 4-tuple. New: `tests/unit/test_channel_routing_guidance.py`, `tests/api/server_channels/server_channels_routing_guidance_test.py`, `tests/api/routing/routing_simulate_guidance_reply_test.py`; READMEs updated. No existing no-match test flipped (empty ballots). Regression test-runner (sonnet) launched on the six groups.
- 2026-09-14 — P2 regression (test-runner ac8fc0b692ccd4616): 2881 passed, 1 failed, 10 skipped in 7:34. Failure by design: `routing_simulate_no_side_effects_test.py::test_simulate_response_is_the_stored_trace_read_back` (simulate now has extra `guidance_reply`). Fresh test-writer launched to adjust it (identical except guidance_reply). **PHASE 2 DONE pending that one test.** Phase 3 (clarification round-trip) developer launched in parallel.
- 2026-09-14 — P2 simulate test adjusted (test-writer a6c9dd7e25774e28d): key sets differ by exactly `guidance_reply`, value asserted None for a routed decision, rest equal; docstring + README row updated; file 14 passed. **PHASE 2 DONE.** Manager lesson: a `grep -c "[p]ytest"` wait loop whose own text contains "pytest" self-matches and never ends (in-container PID 10247) — killed. Never wait on the container that way.
- 2026-09-14 — P3 developed (developer ac7728b065cf06907). New model `channel_routing_clarification.py` + migration `32563e0d9399` (revises d1721ea327b6, applied); new `channel_routing_clarification_service.py` (ask/lookup/claim rowcount/purge/answer receipts); `resolve_choice` + `ClarificationOption` in channel_routing_guidance.py; `MATCH_CLARIFIED`; `decide(..., can_clarify, chosen_ref_id)`; `classify_clarification_reply`; inbound step 7.5 dispatch + clarify effect + receipts + purge on flush tick; simulate asks only if guidance on + named channel supports status notice (schema unchanged, no regen). Decisions: question stays in space, chosen option gets fresh notice; cancel ⇒ `REPLY_CLARIFY_CANCELLED`; files held like parked (24h GC, missing filtered + note); answer with attachments ⇒ new request; name match ≤2 extra words; race loser dropped + debug; Pass 2 clarify allowed. Fresh reviewer + fresh test-writer launched in parallel. Manager-flagged risk for review: classifier-resolved free-text reply that is really a new task routes the ORIGINAL and drops the reply.
- 2026-09-14 — P3 review (reviewer a3d5f2302a672e6dc): CHANGES REQUESTED. F1 BLOCKING (confirmed manager risk): new request as reply lost ⇒ (a) >4 words after filler strip ⇒ discard question, route fresh, no classifier; (b) classifier-resolved reply delivered after the original on the new binding (resolve_choice answers still not ingested). F2 flag off must skip step 7.5. F3 can_clarify also needs outbound credentials (+ simulate). F4 race-loser debug entry `already_answered`. F5 "no"/"no thanks" cancel, prefix ≥3 chars. Confirmed fine: dispatch order, round-trip, redelivery, migration, purge, purity. All relayed to developer ac7728b065cf06907; test-writer a9575d3076eace3f9 told fixed behaviour.
- 2026-09-14 — P3 fixes applied (developer ac7728b065cf06907, resumed): `CLARIFY_SELECTOR_MAX_WORDS=4` + `is_selector_like`; `_deliver_reply_after_original` (lost-race hand-off; parked ⇒ reply behind original; nothing bound ⇒ reply routes fresh); step 7.5 flag-gated; can_clarify + outbound creds (simulate mirrored); `_CLARIFY_ALREADY_ANSWERED` debug; "no"/"no thanks" cancel, prefix/substring ≥3. Reviewer resumed on delta; test-writer told files stable.
- 2026-09-14 — P3 fix re-review (reviewer a3d5f2302a672e6dc): 1a, 1b ordering/locking, 2–5 APPROVED. Remaining: should-fix — original binds nothing ⇒ drop classifier-resolved reply with debug `superseded` (routing it fresh could overwrite the original's new question row / double guidance); nit — "nope", "no thank you" cancel. Relayed to developer + test-writer.
- 2026-09-14 — P3 final fixes applied (developer ac7728b065cf06907): reply dropped with debug `superseded` when original binds nothing (unused `origin` param removed); "no thank you"/"nope" cancel. Reviewer resumed for delta confirm; test-writer told app stable.
- 2026-09-14 — P3 code APPROVED (reviewer a3d5f2302a672e6dc): `origin` removal clean, dropped path already acked. Waiting on P3 test-writer a9575d3076eace3f9. P4 FRONTEND (ui-developer, opus) launched in parallel (frontend/src only; client already current). P4 backend (verdict codes) held until P3 regression is back.
- 2026-09-14 — P3 tests (test-writer a9575d3076eace3f9): NEW `tests/unit/test_channel_routing_resolve_choice.py` (110 passed, before nope/no-thank-you cases), NEW `tests/api/server_channels/server_channels_routing_clarification_test.py` (19 tests: 17 passed, 1 FAILED = real bug, 1 not yet run), helpers `get_routing_clarification`/`expire_routing_clarification` in tests/utils/server_channel.py, guidance test file updated (16 passed). BUG: redelivered answer "2" ingested — receipt written before original ingest, `reset_for_new_session` deletes ingest-log rows (parked path likely same). Relayed to developer ac7728b065cf06907.
- 2026-09-14 — P4 FE built (ui-developer a329067f25fbf4384): routingCopy.ts, routingStages.ts, NEW RoutingGuidance.tsx, RoutingStagesView.tsx (417 lines), RoutingTraceDetail.tsx, RoutingSimulateDialog.tsx; tsc clean for touched files; `guided` in outcome filter; secondary badge. Pre-existing (out of scope): `formatDateTime` parses tz-less UTC as local. ui-designer (review) + fresh code-reviewer launched in parallel on FE.
- 2026-09-14 — P3 redelivery bug FIXED (developer ac7728b065cf06907): `reset_for_new_session` preserves `CHANNEL_INGEST_SOURCE_CLARIFY_REPLY` rows (constant in channel_thread_ingest_log.py); `_install_and_park` writes receipt right after upsert. Reviewer a3d5f2302a672e6dc resumed on this delta; test-writer a9575d3076eace3f9 resumed (re-run failing + unrun tests, unit file, add parked-path redelivery test, confirm ordinary reset still clears).
- 2026-09-14 — P4 FE reviews: ui-designer ad67db5b0c3707ed1 ITERATE (trace list filter trigger clips label ⇒ w-[13rem]; stage detail + simulate PASS; formatDateTime is CORRECT — created_at tz-aware, drop that note); code-reviewer ab30ce0f67aef85b5 APPROVED + suggestions (break-all on matched_pattern code; tiedIsAsked by ids). Relayed 4 fixes (+ reply block below Message) to ui-developer a329067f25fbf4384.
- 2026-09-14 — P3 redelivery fix APPROVED (reviewer a3d5f2302a672e6dc): receipts never read as context, bounded, race holds. **P3 CODE DONE** (tests re-running).
- 2026-09-14 — P4 FE fixes applied (ui-developer a329067f25fbf4384): filter w-[13rem], break-all, tiedIsAsked by ids, reply below Message; tsc clean. Only ITERATE finding had an exact fix ⇒ verified from tree, no further designer round. **P4 FE DONE.**
- 2026-09-14 — P4 backend (verdict codes guided_help/guided_clarify/guided_no_match/clarified) developer ac9d10779611814ac launched early; decision: ONE combined regression for P3+P4 after both are tested (saves a run; each phase still reviewed + tested separately).
- 2026-09-14 — P3 tests GREEN (test-writer a9575d3076eace3f9): redelivery test passes unchanged; superseded test + resolve_choice unit 112 passed; NEW parked-path redelivery test; NEW reset keeps clarify receipt test in test_channel_conversation_regressions.py; final 60 passed. **P3 DONE** (pending combined regression).
- 2026-09-14 — P4 backend developed (developer ac9d10779611814ac): routing_reachability_service.py — `guided_help`, `guided_clarify`, `guided_no_match`, `clarified` + two `expected_agent_considered` sentences. Breaks by design: verdict_test `...classifier_matched_none` and `...expected_agent_was_eligible_but_not_picked`. Launched in parallel: reviewer a3d5f2302a672e6dc (resumed) on verdicts; fresh test-writer for verdict tests; ui-developer a329067f25fbf4384 (resumed) for diagnosisTone; documenter (sonnet) for all-phase docs + sync-platform-knowledge.
- 2026-09-14 — P4 FE tones done (ui-developer): clarified/guided_help ok, guided_no_match warn, guided_clarify neutral. P4 verdict review (reviewer a3d5f2302a672e6dc): CHANGES REQUESTED — should-fix guided_help remedy wrong for pass_2 bundle list; nits: clarified + no_match branch (picked option vanished), guided variant for expected agent not among listed options. Relayed to developer ac9d10779611814ac; test-writer a31054c4c1f0d9b14 + documenter a745a052e395fc960 told to hold those sentences.
- 2026-09-14 — P4 verdict fixes applied (developer ac9d10779611814ac): pass_2 bundle-list variants for guided_help/guided_clarify/guided_no_match; new `clarified_unavailable`; guided "not among listed options" sentence under expected_agent_considered. Reviewer resumed for delta confirm; test-writer + documenter told stable; ui-developer asked for `clarified_unavailable` → warn.
- 2026-09-14 — P4 verdict fixes APPROVED (reviewer a3d5f2302a672e6dc); FE `clarified_unavailable` → warn done. **ALL CODE DONE AND REVIEWED.** Remaining: verdict test-writer a31054c4c1f0d9b14 → combined P3+P4 regression; documenter a745a052e395fc960.
- 2026-09-14 — P4 tests GREEN (test-writer a31054c4c1f0d9b14): routing_reachability_verdict_test.py 42 passed; 2 by-design tests run with guidance off; 12 new (agent + bundle variants, clarified routed/parked via webhook, clarified_unavailable seeded, 3 expected_agent sentences); `seed_routing_trace(match_method=)`. Final combined regression (sonnet) launched, split into two foreground invocations.
- 2026-09-14 — FINAL REGRESSION GREEN (test-runner a04a5a60f0c181aaa): A server_channels+identity+app_mcp 272 passed (4:39); B architecture+unit+routing 2751 passed, 10 skipped (3:45). 0 failed.
- 2026-09-14 — DOCS DONE (documenter a745a052e395fc960): server_channels.md/_tech, routing_tuning.md/_tech, agent_prompts.md, identity_routing.md, app_mcp_server.md, docs/README.md glossary + regenerated registry, CAPABILITIES_AGENT.md row; check-docs pass; sync_platform_knowledge clean (258 files).
- 2026-09-14 — **FEATURE COMPLETE, UNCOMMITTED.** Full backend suite NOT run (user runs `make test-backend`). Open: manual check of list rendering in a real Google Chat space; UI not eyeballed (no screenshots required).
